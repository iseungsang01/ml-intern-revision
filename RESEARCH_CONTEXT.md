# KSTAR CES Nowcasting — 연구 컨텍스트 (자족 문서)

이 문서는 **저장소 접근 없이 이것 하나만 읽고** 연구 전체와 각 실험의 **설계·결과·판정**을 이해할 수
있도록 쓰였다. 대상 독자는 연구자다: 도메인 상식은 최소한만 깔고, 대신 "어떻게 실험했는가"를 본문으로 둔다.

모든 수치는 얼린 실행 산출물에서 수집 스크립트가 뽑아낸 값이며, 저장소 안에서는
`THESIS_RESULTS.md`(실험 원장) · `PROJECT_KNOWLEDGE.md`(장기 교훈) · `paper_numbers.json`(수치 단일
출처)이 원본이다. 이 문서와 충돌하면 그쪽이 이긴다.

> **2026-08-12 방향 재정립 — 전면 재실험 (THESIS_RESULTS.md §8v).** 확정 프로토콜은
> **W = 2 · held-free(genuine) · `T_i` fit-실패 스파이크 사전등록 제외** 단일 체제다. 이 문서의
> §6–§7 정량 수치는 전부 W = 4 프로토콜의 **잠정치이며 폐기·교체 대상**이다(헤드라인, MNAR 재가중,
> 캠페인 분할, conformal, 해석가능 사다리 포함). 교체 전까지 수치를 인용하지 말 것 — 이 문서 안에서
> 수치는 실험 설계·판정의 **기록**으로만 읽는다. 실행 순서: **B.7 프로토콜 감사 → B.1 백본
> 관문(seq_v2 × 16 seed + 학습 예산 균등화) ∥ 인과(past-only) GP 기준선 → B.2 모델 탐색(test 동결)
> → B.3 최소 해석가능 모델 → B.4 스케일링 상한 → B.5 전면 재채점**; B.6(kHz Mirnov 특징, 모드 회전
> 주파수 포함)은 도착 시 비동기. "즉시 반응형 실시간" 주장은 **마이크로초 추론 + kHz 입력 결합**이
> 전제이며, 어느 한쪽이라도 불가로 판정되면 주장을 제거하고 기여를 재서술한다.

---

# 1. 배경과 연구 질문

## 1.1 무엇을 예측하는가

토카막 플라즈마 가장자리의 **이온 온도 `T_i`** 와 **토로이달 회전 `V_rot`** 은 pedestal 구조, ELM
거동, 고구속 모드(H-mode) 진입을 좌우한다. 두 양의 표준 측정 수단이 **CES(Charge Exchange
Spectroscopy)** 로, 전하교환 발광선의 **Doppler 폭**에서 `T_i`를, **Doppler 이동**에서 `V_rot`를 얻는다.

문제는 CES가 **본질적으로 느리다**는 것이다. 충분한 S/N을 얻으려면 광자를 적분해야 하고, 노출·신호품질
문제로 개별 시점이 자주 유실된다. 이 연구가 쓰는 KSTAR 데이터에서는 균일한 **10 ms 격자** 위에서도
`T_i`의 8.2%, `V_rot`의 23.9%가 결측이며 — **서로 독립적으로** — 여기에 더해 `V_rot` 행의 41.1%가
직전 값을 그대로 반복한다. 결과적으로 격자의 **35.0%** 만이 독립적인 `V_rot` 측정을 담는다.

게다가 결측은 **물리적으로 흥미로운 순간에 몰린다**(저 S/N 구간, ELM, 천이). 즉 통계적으로
**MNAR(missing not at random)** 이다. 이 사실이 이 연구의 평가 설계 전체를 규정한다.

## 1.2 무엇으로 예측하는가

같은 플라즈마를 **끊김 없이** 관측하는 빠른 진단이 세 종류 있다. 셋 다 `T_i`나 `V_rot`를 **직접 재지
않는다.**

| 진단 | 채널 | 무엇을 재는가 |
|---|---:|---|
| **BES** (Beam Emission Spectroscopy) | 9 | 밀도 요동 구조 (`n_e` 계열) |
| **ECEI** (Electron Cyclotron Emission Imaging) | 4 | 전자 온도 영상 (`T_e` 계열) |
| **MC** (Mirnov coils) | 2 | 자기 요동 `dB/dt` |

## 1.3 연구 질문

> **CES가 비어 있는 10 ms 시점에서, 같은 시각의 빠른 진단과 (불규칙한) 과거 CES 이력만으로,
> CES 자체의 시간 보간이 복원할 수 없는 `T_i`·`V_rot` 정보를 회복할 수 있는가?**

긍정이면 **데이터 기반 가상 센서**가 정당화된다 — 하드웨어 대안이 요구하는 강한 역사상(inverse
mapping) 가정 없이 CES 공백을 온라인으로 채우고, 끊김 없는 pedestal 분석과 궁극적으로 실시간 사용을
가능하게 한다.

프레이밍 주의: 이것은 **forecasting(미래 예보)** 도 **super-resolution** 도 아니다. 지금 비어 있는
값을 채우는 **nowcasting**이다.

## 1.4 일부러 불리한 잣대

공백 채우기의 자명한 기준선은 CES 채널 자체의 보간이다. 이 연구는 비교를 **의도적으로 모델에 불리하게**
설정한다:

- **보간 기준선들은 오프라인**이다 — 타겟 시각의 **양쪽**, 즉 **미래** CES 이웃까지 볼 수 있다.
- **모델은 엄격히 인과적**이다 — 타겟 시점의 빠른 진단과 **과거** CES 이력만 본다.

인과 모델이 미래를 보는 보간을 이긴다면, 빠른 진단이 **CES의 시간 구조만으로는 얻을 수 없는** 정보를
실제로 운반한다는 뜻이다. 그리고 미래를 쓰는 보간을 이기는 모델은 인과 기준선(persistence, AR)을
자명하게 이긴다.

## 1.5 결과로 확립된 두 개의 주장 (절대 섞지 말 것)

| | 주장 | 어떤 집단에 대한 진술인가 | 상태 |
|---|---|---|---|
| **오프라인 주장** | 미래를 쓰는 보간을 이긴다 | **관측된** CES 점 | `T_i`만 성립(4/4). 단 GP와는 무승부 |
| **인과/배치 주장** | 배치 가능한 모든 인과적 방법을 이긴다 | **실제로 결측인** 점으로 재가중해도 생존 | 두 타겟 모두 성립(`T_i` 4/4, +0.29) |

온라인 가상센서의 경쟁자는 persistence이지 미래를 읽는 보간이 아니다. 이 둘을 섞는 것이 이 결과를
과대판매하는 유일한 경로이며, 실험 두 건(§7.6 MNAR, §7.10 campaign)이 정확히 이 분리를 위해 존재한다.

---

# 2. 데이터

## 2.1 구성

- KSTAR 방전(shot) **641개**. 하드웨어 일관성을 위해 캠페인 #24000–#33000에서 우선 선별, H-mode
  ELM 억제(RMP) 구간을 겨냥했고, D_α 활성 구간 주변 ~100 ms 절편을 잘라 썼다.
- 모든 신호를 공통 **10 ms 격자**에 정렬. 총 **247,207 행**.
- 행당 컬럼 18개: `time`, `CES_TI`, `CES_VT`, `BES_*`×9, `ECEI_*`×4, `MC*`×2.
- 한 shot 파일은 **≥0.5 s 간극으로 분리된 여러 측정 세그먼트**를 담는다(641 shot 전부; 파일당 중앙값
  2개, 간극 중앙값 6.3 s). 보간도 모델 입력 윈도우도 **세그먼트 경계를 넘지 않는다.**
- 단위: `CES_TI`는 **eV**(RMSE가 372–540 수준). `CES_VT`는 원자료 단위 그대로 보고한다.

## 2.2 결측 장부 (641파일 전수 조사)

| | `CES_TI` | `CES_VT` |
|---|---:|---:|
| ① NaN 결측 | 20,216 (8.2%) | 59,107 (23.9%) |
| ② held / forward-fill (직전 관측과 bit-identical) | 1 (0.0%) | 101,604 (41.1%) |
| **실질 결측 ①+②** | **20,217 (8.2%)** | **160,711 (65.0%)** |
| 독립 관측 | 226,990 (91.8%) | 86,496 (35.0%) |
| 관측값 중 held 비율 | 0.0% | **54.0%** |
| held run 포함 shot | 1 / 641 | 499 / 641 |
| held run 길이 (중앙/최대) | 2 / 2 행 | 10 / **1,214** 행 |

> **"`CES_VT`는 24% 결측"을 단독으로 인용하면 안 된다.** 독립 정보가 없는 격자 비율은 **65.0%** 다.

## 2.3 데이터에 박혀 있는 네 가지 사실 — 실험 여러 개가 여기서 나왔다

**(a) held(forward-fill) 값.** `V_rot`의 고유 계측 주기가 행 주기보다 느려서 값이 그대로 이월된다.
독립 측정이 아니다. **평가에서 제외**하는 것이 옳다 — forward-fill을 재현하는 건 측정이 아니고,
직전 값을 이월하는 arm(무엇보다 persistence)은 그것을 공짜로 맞힌다. 이 보정은 보고되는 물리
`V_rot` RMSE를 **35–55% 부풀리고**(22–33 → 35–46) 채점 대상 `V_rot` 집단을 약 60% 줄인다.
더 나아가 held는 **학습까지 오염**시킨다(§7.1).

**(b) CES 스펙트럼 fit 실패.** `CES_TI` > ~3 keV 값은 물리가 아니라 피팅 실패다(전역 p99 = 2,089 eV,
최대 14,984 eV). 모든 arm에게 예측 불가능하고 제곱오차가 거대해서 MSE 비를 1로 끌어당긴다 →
남겨두면 skill이 **깎인다**(§7.12).

**(c) Mirnov 앨리어싱.** kHz `dB/dt`를 anti-aliasing 필터 없이 100 Hz로 데시메이트했다. 같은 격자
위에서 블록 내 lag-1 자기상관을 재면:

| 진단 | 블록 수 | 평균 r | 중앙 r | \|r\|<0.1 비율 |
|---|---:|---:|---:|---:|
| BES | 5,886 | +0.568 | +0.647 | 7.6% |
| ECEI | 2,598 | +0.572 | +0.653 | 9.2% |
| **MC (Mirnov)** | 1,308 | **−0.009** | **−0.004** | **82.0%** |

BES/ECEI는 시간적으로 연속인데 MC만 백색잡음이다. 정확한 진술은 느슨한 의미의 "앨리어싱"이 아니라:
**연속 샘플의 상대 위상이 무작위가 되어 진폭·모드수 정보가 모델에 도달하기 전에 파괴되었다.**
따라서 파생 MC 특징(적분, PCHIP 적분, |MC|, rolling RMS)으로는 복구되지 않는다 — 실제로 시도했고
전부 음성이었다. 복구하려면 **원본 kHz 스트림**에서 윈도우 RMS / band power / 모드수를 뽑아야 한다.

**(d) NBI 토크 채널의 부재.** `V_rot`를 실제로 구동하는 양이 데이터셋에 **아예 없다**.
`T_e`(ECEI 평균)로 대리하려는 시도는 데이터가 반증한다. 각 타겟을 20회 이상 관측한 538 shot에서:

| 관계 (shot 간) | Pearson r | p |
|---|---:|---:|
| `T_e` ~ `CES_TI` | **+0.353** | 2.9e−17 |
| `T_e` ~ `CES_VT` | +0.024 | 0.58 |
| `T_e` ~ \|`CES_VT`\| | +0.001 | 0.98 |

shot 내부에서도 같다: `T_e`~`CES_TI`는 평균 r = +0.246에 **부호가 일관**(블록의 42.7%가 |r|>0.3),
`T_e`~`CES_VT`는 평균 +0.006에 **부호 자체가 무작위**(14.8%). 사슬의 앞 절반(NBI→전자가열→`T_e`)은
데이터가 확인해 주기 때문에 뒤 절반의 null이 정보가 된다. 끊기는 지점은 **power ≠ torque**:
토크는 빔 에너지·접선반경·주입 기하에 의존하고(파워와 분리된다), 회전은 국소 `T_e`가 아니라 운동량
수송과 edge braking(NTV, error field, 벽 항력)이 지배한다.

---

# 3. 샘플 구성과 학습 설정

실험들이 조작하는 대상이 여기다. 이 절을 모르면 §7을 읽을 수 없다.

## 3.1 한 학습 샘플

히스토리 윈도우 `W = 4`, 타겟 인덱스 `t`일 때 한 샘플은:

```
bes            (W, 9)     ecei           (W, 4)     mc             (W, 2)
time_features  (W, 4)     ces_history    (W, 4)
target         (2,)   = 정규화된 [CES_TI, CES_VT]
target_mask    (2,)   ∈ {0,1}²  — 타겟별 관측 여부
```

- **시간 특징 4채널** = lookback 초, 행간 delta 초, 그리고 각각의 `log(1+x)`. 불규칙 관측 패턴을
  모델에 **명시적으로** 노출한다 — 과거 CES 값의 신뢰도는 그것이 10 ms 전인지 200 ms 전인지에 크게 의존한다.
- **`ces_history` 4채널** = 이전 정규화 `CES_TI`, 이전 정규화 `CES_VT`, `CES_TI` 관측 flag,
  `CES_VT` 관측 flag. 두 타겟이 **독립적으로** 결측되므로 관측은 타겟별로 추적해야 한다.
- **타겟 행은 윈도우의 마지막 행**이며, 그 시점의 값과 flag는 **모두 0으로 완전 마스킹**된다.

## 3.2 설계 원칙 세 가지

- **라벨을 만들어내지 않는다.** 학습 행을 만들기 위해 타겟을 절대 대치(impute)하지 않는다. 행은
  진단 입력이 완전하고 **CES 타겟 중 최소 하나가 진짜로 관측**되었을 때만 유지한다.
- **타겟별 masked loss.**
  `L = Σ_k m_k (ŷ_k − y_k)² / Σ_k m_k`, `k ∈ {T_i, V_rot}`.
  한쪽만 관측된 행도 그쪽은 지도한다. 두 타겟 모두를 요구하던 이전 필터는 **라벨 행의 ≈28%를 조용히
  버리고 있었다** — 이걸 제거한 건 순수한 데이터 이득이다.
- **누출 차단 3종.** ① **파일(shot) 단위 split** — 한 방전 내 인접 행은 강하게 자기상관이므로 행
  단위 split은 누출이다. split은 디스크에 고정된다. ② **train 파일만으로 정규화** — 모든 z-score
  통계를 train 파일에서만 추정하고, 성긴 타겟은 **NaN-aware**로 계산한다. ③ **타겟 시점 마스킹** —
  `ces_history`에서 타겟 행의 값 **과** 관측 flag를 0으로 만들어, 모델이 자기 답을 읽지 못하게 한다.

## 3.3 temporal subset 증강 — 그리고 그 폭발

한 타겟 행을 **연속 시간 블록 안의 과거 행들의 여러 불규칙 부분집합**과 짝지어 증강한다(타겟 행은
여전히 마스킹). 이것이 불규칙 샘플링에 대한 강건성을 준다.

대가는 조합 폭발이다: 데이터셋 샘플 수가 **W = 2에서 240k, W = 8에서 30.1M**(파일당 최대 187k).
그래서 샘플 상한이 필수이고, 윈도우 비교 실험에서는 **shot당 상한**까지 따로 필요해진다(§7.4).

## 3.4 학습 설정과 통제 스위치

기본 프로토콜: **10 epoch, batch 512, lr 1e-3**, AdamW, `ReduceLROnPlateau`, gradient clipping,
그리고 MSE 위에 **정규화 0 미만의 `CES_TI` 예측을 억제하는 소프트 페널티**(`0.1 · relu(...)`).

실험들이 조작하는 통제 변수(전부 환경변수로 노출되어 있다):

| 스위치 | 의미 | 어디서 쓰였나 |
|---|---|---|
| held 제거 | held 값을 로드 시점에 NaN 처리 (타겟·이력·정규화 통계·보간 앵커 전부에서) | §7.1 이후 **모든 신규 학습의 기본값** |
| 윈도우 `W` | 히스토리 길이 | §7.4 |
| 입력 ablation | BES/ECEI/MC 그룹 또는 이력을 0으로 | §6.3, §7.4의 history-0 |
| shot별 표준화 | BES/ECEI/MC를 **각 방전 안에서** z-score (**타겟은 손대지 않음**) | §7.13 |
| shot당 샘플 상한 | 전역 상한 **이전에** shot별로 시드 부분집합 | §7.4 (필수) |
| 파일 split 고정 | 다른 실행의 manifest를 그대로 상속 | 페어링하는 모든 배치 |
| 모델 파일 지정 | 아키텍처 변형을 별도 파일로 주입 | §7.7 |

> **비협상 규칙: 데이터 처리를 매 실행마다 명시적으로 고정한다.** 상속하거나 기본값에 맡기지 않는다.
> 조용한 기본값 폴백이 윈도우 스윕의 결론을 통째로 틀리게 만든 적이 있다(§7.4).

---

# 4. 모델

**201,258 파라미터**의 컴팩트한 late-fusion 네트워크. 이 문제의 병목은 용량이 아니라 정보다(§7.4가 증거).

**진단별 인코더.** BES / ECEI / MC 각각을 자기만의 **시간 인지 1-D CNN**이 인코딩한다(Conv–BatchNorm–GELU
2블록 → global average pooling → 96차원 선형 사영). 각 진단의 채널 구조를 융합 직전까지 보존한다.
시간 특징만 따로 먹는 네 번째 작은 CNN이 있다. **센서 스트림에 RNN이 아니라 conv를 쓴 이유**: 결측 행
제거로 실효 샘플링이 불규칙해져서, RNN이 의존하는 균일 스텝 가정이 깨진다.

**히스토리 인코더 — 관측 마스킹 attention pooling (유일한 비범용 요소).**
`ces_history`를 시간 특징과 이어붙여 **단층 양방향 GRU(hidden 64)** 가 읽고, 그 시점별 출력을
**독립적인 multi-head additive attention readout 2개**가 pooling한다 — 하나는 `T_i` head로, 하나는
`V_rot` head로 라우팅된다.

> 양방향인데 왜 누출이 아닌가: **타겟 행이 윈도우의 마지막 행**이라 윈도우 자체가 엄격히 과거다.
> 양방향성은 히스토리 내부에서만 작동하며 미래 접근을 주지 않는다 — 각 히스토리 행을 윈도우 안에서
> 그 뒤에 오는 행들의 맥락 속에서 저울질하게 해줄 뿐이다.

핵심은 **pooling 마스크**다. 각 타겟의 softmax 직전에, **그 타겟의** 관측 flag가 0인 모든 시점
(완전 마스킹된 타겟 행 + 그 타겟이 관측되지 않은 히스토리 행)을 −∞로 보낸다. 그래서 attention 질량은
**그 타겟이 진짜로 측정된 행에만** 놓일 수 있다. 두 타겟은 독립적으로 결측되므로 마스킹도 독립이며,
윈도우 안에 관측 행이 하나도 없는 타겟은 전 구간 attention으로 폴백한다(전부 −∞인 softmax 방지).

**이것이 보간의 귀납편향을 attention에 새겨 넣은 것이다.** PCHIP과 linear가 강한 이유는 공백을 사이에
둔 **관측된 샘플만** 쓰고 공백 자체는 절대 쓰지 않기 때문이다. 마스크 없는 attention pool은 0으로
채워진 타겟 행과 측정이 없는 히스토리 행에 가중치를 낭비하고, 신경망은 그 억제를 flag 채널을 통해
**암묵적으로** 배워야 한다. 하드 마스킹은 같은 편향을 **파라미터 비용 0**으로 직접 공급한다 —
바뀌는 것은 오직 *어느 시점에 가중치를 줄 수 있는가* 뿐이다.

**물리 기반 타겟별 라우팅.** `T_i` head는 빠른 진단 + 시간 + 이력을 융합한다. `V_rot` head는
**이력과 시간만** 융합하고 빠른 진단을 **의도적으로 배제**한다 — ablation이 빠른 진단에 10 ms에서
쓸 만한 회전 정보가 없음을 보였기 때문이다(§6.3). head 크기는 입력에 맞춰 `T_i` 72k / `V_rot` 14k.
출력은 정규화 단위로 남고, 역정규화는 평가에서만 한다.

**아키텍처 탐색에서 얻은 교훈.** 두 라운드에 걸친 ~40회 통제 반복에서: **정규화 배치**가 앞 라운드의
안정성 전환점이었고(검증 손실 0.89 → 0.49), **attention pooling이 가장 신뢰할 만한 단일 개선**이자
최종 모델까지 살아남은 유일한 메커니즘이었으며, **공격적 용량 확대·복잡한 skip 위상·추가 conv
추출기는 일관되게 실패**했다. 이 음성 결과 자체가 정보다 — 다음 이득은 용량 엔지니어링에 없다는 뜻이다.

---

# 5. 평가 프로토콜

## 5.1 기준선 사다리 — 각 arm이 무엇을 볼 수 있는가

| arm | 접근 권한 | 정의 |
|---|---|---|
| **모델** | 빠른 진단(타겟 시점) + **과거** CES | 인과적 nowcaster |
| persistence | 마지막 관측값 | 인과적. 배치 시스템의 실제 경쟁자 |
| AR (local) | **과거** CES만 | 인과적 참조 |
| linear | 과거 **+ 미래** CES 이웃 | 오프라인 |
| **PCHIP** (헤드라인) | 과거 **+ 미래** CES 이웃 | 오프라인. 단조 3차 보간 |
| **GP** | 과거 **+ 미래** CES 이웃 | 오프라인 최강 (§7.11에서 추가) |

보간은 **타겟 자신의 값을 제외**하고(누출 방지), 세그먼트 경계(≥0.5 s 간극)를 넘지 않으며, 경계 너머
이웃이 필요한 경우 **persistence 값을 대신 예측**한다(PR2 — 어느 arm도 표본이 얇아지지 않는다).

## 5.2 지표

물리 단위로 역정규화한 뒤 **타겟별로** 계산한다. 주 지표는 Murphy skill:

```
skill_vs_pchip = 1 − MSE_model / MSE_PCHIP        (양수 = 모델 우세, 0 = 무승부)
```

모든 arm(모델 + 전 기준선)은 **동일한 `(file, row)` 샘플 집합**에서, **동일한 타겟별 keep 마스크**로 채점된다.

## 5.3 3-way split과 TEST 격리

파일을 train/val/**test**로 나눈다. **TEST는 아키텍처 탐색 이전에 예약되었고 모델 선택 중에 한 번도
읽히지 않았다** — 따라서 헤드라인 숫자에 winner's-curse 편향이 없다. seed 42/1/7/123 중
**1/7/123은 선택에 전혀 쓰이지 않았으므로 4/4 PASS는 선택 밖 복제**다.

기준 split(seed 42) TEST 규모: 채점 샘플 **33,693**, 관측 `T_i` **n = 32,787 / 96 shot**,
genuine `V_rot` **n = 10,729 / 61 shot**. `V_rot` 수가 작은 건 held를 제외했기 때문이며, held를
측정처럼 채점하면 27,437로 부풀지만 그건 민감도 열로만 보고한다.

## 5.4 사전등록(PR) — TEST 숫자를 보기 전에 문서로 고정

- **PR1** 헤드라인 비교 대상은 **PCHIP**(천이에서 단조성/링잉 없음을 이유로 사전 선택). 전체 사다리도 보고.
  *정직한 방어*: PCHIP은 이 데이터에서 linear보다 **약하다**(`T_i` RMSE 431.81 vs 422.66) —
  Δt ≤ 15 ms가 채점 샘플의 97.7%라 3차항이 도울 여지 없이 미분 추정 잡음만 더하기 때문이다. 방어는
  "PCHIP이 최강"이 아니라 **"사전등록했고, 더 강한 linear로 바꿔도 결론이 유지된다"**(4/4 PASS)이다.
- **PR2** 보간은 모델이 채점되는 모든 곳에서 예측하고 persistence로 폴백한다(표본 축소 없음).
- **PR3** TEST 하한 ≥15 shot & ≥3,000 관측 `T_i` 샘플 (충족).
- **PR4** 유의성 = **shot-clustered paired bootstrap의 95% CI가 0을 배제**.

## 5.5 shot-clustered paired bootstrap

한 방전 안의 인접 CES 행은 강하게 상관되어 있어, 샘플을 독립으로 취급하면 불확실성을 크게 과소평가한다.
그래서 샘플별 paired 오차 `SE_model − SE_PCHIP`를 **shot 단위로 집계하고, 통째 shot을 복원추출**한다
(10,000회, 고정 시드). 이 CI는 진짜 **shot 간 일반화**를 반영한다. 대가는 **유효 표본이 shot 수(~96)**
라는 것이고, 이것이 이 연구 전체의 검정력 상한이다.

split seed 간 변동은 부차적 안정성 점검이며 헤드라인 CI에 합치지 않는다.

## 5.6 평가 처리 두 가지 — 항상 함께 본다

- **`genuine`** = held 제외 (**헤드라인**)
- **`stuck0`** = held 포함 (역사적 관행; `V_rot` RMSE를 35–55% 깎아내림)

하나를 인용하면서 다른 하나를 주장하는 오류가 실제로 발견된 적이 있어서, 수치 수집 스크립트가 **두 처리
모두**를 산출하게 만들어 두었다.

## 5.7 MNAR 문제와 대응

진짜 결측점에는 참값이 없다. 그래서 **관측된 점을 일부러 가려** skill을 잰다. 결측이 MNAR이므로
관측점 skill은 **낙관적 추정**이다. "그렇다고 말하고 멈추면" 핵심 숫자에 크기를 모르는 보정이 붙어
있는 셈이라, 멈추지 않고 §7.6에서 **재가중으로 정량화**한다.

## 5.8 실험에 건 운영 제약과 그 이유

아래는 스타일 규칙이 아니라 **비협상 조건**이고, 각각 **실제로 잘못된 결론을 만든 사건에서** 나왔다.
새 배치를 설계한다면 이 목록이 체크리스트다.

**C1. 데이터 처리를 러너의 환경 딕셔너리에 명시적으로 고정한다 — 상속도, 삭제도, 기본값도 금지.**
막는 것: held 제거 여부가 조용히 기본값으로 떨어지는 것. **실제 피해**: 윈도우 스윕의 첫 패스가 이 버그로
"`V_rot`는 긴 이력이 필요하다"(+0.118 → +0.202)는 결론을 냈는데, held-free로 다시 돌리니 기울기가 통째로
사라졌다(§7.4). 같은 버그가 anchor 배치에도 실려 있다가 실행 직전에 잡혔다.

**C2. 파일 split을 control의 manifest에 고정하고, test 격리를 별도로 검사한다.**
막는 것: 처리 변경이 유효 파일 목록을 바꾸면 시드 셔플이 **재분할**되어 control의 test shot이 실험 arm의
학습으로 새는 것. **실제 피해**: held 제거 실험에서 seed 7/123에 이 누출이 관측됐고, 그래서 split 고정
스위치가 추가됐다(§7.1). manifest에 없는 파일(상한에 잘려나간 파일)은 학습에서도 제외한다.

**C3. control은 반드시 같은 데이터 처리로 학습된 arm이어야 한다.**
막는 것: paired 차이에 데이터 처리가 **두 번째 통제되지 않은 변수**로 섞이는 것. held-free arm은
held-free control과, held-kept arm은 held-kept control과 짝짓는다. §7.7(anchor)과 §7.13(per-shot Part 2)이
바로 이 이유로 control을 바꿨다.

**C4. 두 arm이 같은 행을 같은 순서로 채점했는지 검증하기 전에는 비교하지 않는다.**
페어링 도구가 이걸 강제한다(shot과 Δt의 bit-identity, 기준선 오차의 허용오차 내 일치). 검증 없는
paired bootstrap은 서로 다른 모집단의 차이를 신뢰구간으로 포장한다.

**C5. 얼린 실행을 재채점할 때는 키를 가산적으로만 추가하고, 기존 키 전부가 bit-identical로 재현되는지
먼저 확인한다.**
막는 것: 메타데이터를 추가하는 패스가 발표된 숫자를 **밑에서 바꿔치는** 것. 이 패턴을 §7.5(persistence 키),
§7.6, §7.11(GP arm)에서 썼고 매번 통과했다 — 통과했다는 사실이 새 키를 신뢰할 수 있게 만드는 근거다.
새 arm을 추가할 때는 그 arm의 NaN 조건이 기존 arm과 동일한지까지 확인한다(그래야 모집단이 줄지 않는다).
**한 가지 예외**: 이 기계에서 float32 CUDA forward는 세션 간 bit-재현되지 않는다(샘플별 상대 드리프트
중앙값 3e−4). 그래서 **모집단 키**(shot, Δt, peak 플래그, 기준선 오차)는 bit-identical을 요구하고,
**모델 오차 키**만 bounded-drift(RMSE < 0.01 물리단위)로 검사하며, 병합 산출물은 **참조 모델 오차를 유지**한다.

**C6. 비교를 교란할 수 있는 축은 가정하지 말고 실측한다.**
윈도우 스윕이 표준 사례다: split이 윈도우 불변인지 직접 확인하고, 24회 실행이 모두 동일한 96 test shot을
쓰는지 사후 확인하고, 평가 모집단 축소가 −1.8%임을 재고, 증강 폭발을 막기 위해 **shot당 샘플 상한**을
새로 도입했다(§7.4). 이 세 통제가 없으면 "W가 클수록 좋다"가 표본 구성의 부산물로 나온다.

**C7. 한 번에 한 변수. 4개 독립 seed. shot-clustered paired bootstrap. 판정 규칙은 실행 전에 정한다.**
단일 seed 결과는 증거가 아니다. 그리고 판정 규칙을 미리 정했기 때문에, `seq_v2`처럼 **좋아 보이는 결과에
러너가 FAIL을 반환**하는 일이 실제로 일어난다(§7.3) — 그게 규칙이 작동한다는 증거다.

**C8. 결과가 어느 쪽이든 기록한다.** 음성 결과의 기록이 코드보다 오래 남고, 재시도를 막는 것은 코드가
아니라 기록이다(§7.14).

---

# 6. 핵심 결과

## 6.1 헤드라인 (held-out TEST, `skill_vs_pchip`, genuine 평가)

| target | seed 42 | seed 1 | seed 7 | seed 123 | 평균 | PR4 |
|---|---:|---:|---:|---:|---:|---|
| **`CES_TI`** | **+0.179** | **+0.197** | **+0.280** | **+0.263** | **+0.230** | **4/4 PASS** |
| `CES_VT` | +0.203 | +0.162 | +0.100 | +0.183 | +0.162 | 1/4 (seed 1만) |

`stuck0` 처리에서는 `T_i` +0.257 / +0.194 / +0.263 / +0.280 — 역시 4/4 PASS. **두 처리가 두 결론 모두에
동의한다.** `V_rot`의 1/4는 잡음만으로도 나올 수 있는 값이므로 **무승부로 보고**한다.

## 6.2 RMSE 사다리 (seed 42, 물리 단위, `stuck0`)

| arm | `CES_TI` | `CES_VT` |
|---|---:|---:|
| **모델** | **372.32** | **22.53** |
| linear | 422.66 | 24.01 |
| PCHIP | 431.81 | 24.49 |
| persistence | 487.31 | 27.77 |
| AR (local) | 1005.66 | 57.23 |

모델이 **두 타겟 모두 사다리 최저**. genuine 평가에서 `V_rot`의 실제 RMSE는 ~35–47이다(위 22.53은
held 때문에 눌린 값).

## 6.3 비대칭의 메커니즘 — 입력 modality ablation (val, `skill_vs_persistence`)

| ablation | `CES_TI` | `CES_VT` |
|---|---:|---:|
| Full (이력 + 빠른진단 + 시간) | +0.428 | +0.296 |
| `no_fast` (이력만) | +0.458 | +0.295 |
| `no_history` (빠른진단만) | **+0.372** | **−0.642** |

빠른 진단만으로도 `T_i`는 persistence를 크게 이긴다(+0.372) — 충돌 e–i 결합으로 ECEI가 보는 `T_e`와
BES가 보는 `n_e`가 `T_i`를 제약하므로 **물리적으로 예측되는** 결과다. 같은 모델이 `V_rot`에서는
persistence보다 **나쁘다**(−0.642, 평균 예측보다도 못함): 회전 정보는 사실상 전부 과거 CES 이력에서
온다. **이것이 `T_i` ↔ `V_rot` 정보 비대칭의 메커니즘이며, 아키텍처가 아니라 물리다.**

## 6.4 skill이 어디에 사는가

- **고변동("peak") 이웃에 집중.** peak는 **타겟 자신의 값을 제외한 입력만의 프록시**(이웃 bracket
  기울기 / 국소 이웃 분산)로 정의되어 비순환적이다. val 기준 `T_i` 전역 +0.272 → peak **+0.702**
  (PASS), `V_rot` +0.131 → **+0.438**(PASS). 보간은 매끄러운 본류에서 거의 최적이고, 모델의 값어치는
  활동 구간에 있다.
- **간극(Δt)별**은 §7.5 참조 — 4 split을 pooling하면 `T_i`는 Δt > 15 ms 전체에서도 미래를 쓰는 PCHIP을 이긴다.
- **`V_rot`는 하나의 판정이 아니라 세 영역**(§7.12 참조).

---

# 7. 실험 대장 — 설계와 판정

각 항목: **왜 → 어떻게(설계) → 결과 → 판정 → 무엇을 보여주지 않는가.**
공통 제약은 §5.8(C1–C8)을 따르며, 아래에서는 배치별로 **다른 점**만 적는다.

## 7.0 설계 골격 한눈에

| § | 배치 | 통제 변수 | control arm | split 처리 | 복제 | 사전 판정 규칙 |
|---|---|---|---|---|---|---|
| 7.1 | stuck-free | held 제거 on/off (학습+평가) | held-kept 계열 | control manifest에 고정 + 누출 검사 | 4 split seed | paired `V_rot` 4/4 양수 & 다수 CI가 0 배제 |
| 7.2 | seq v1 | 프레이밍: 윈도우 → 전체 격자 | 윈도우 모델 (held-kept·held-free 양쪽) | 동일 파일 split | 4 | paired 4/4 우세 |
| 7.3 | **seq v2** | v1 + `V_rot` 인코더 라우팅 + shot별 표준화 | **held-free** 윈도우 계열 | 동일 | 4 | paired 4/4 우세 (→ 실제로 **FAIL** 반환) |
| 7.4 | 윈도우 스윕 | `W` ∈ {2,3,4,6,8} + history-0 | 없음(각 실행의 자기 skill) | 윈도우 불변 확인, 24실행 동일 96 test shot | **24 run** (6점 × 4 seed) | 자기 PR4 PASS 수 + "plateau 도달 최소 W" |
| 7.5 | large-gap | 없음 (재채점, 인과 기준선 추가) | persistence 키 가산 추가 | 얼린 split, 4 split **pooled**, 물리 shot 클러스터 | 4 | 구간별 shot-clustered CI |
| 7.6 | MNAR | 없음 (사후층화 재가중) | — | 얼린 split | 4 | 재가중 skill의 CI + 층 커버리지 보고 |
| 7.7 | anchor | 아키텍처 (1,258 param) | **held-free** 계열 | control manifest 고정 + 누출 검사 | 4 | paired 4/4 우세 (→ **FAIL**, 회수율은 별도 보고) |
| 7.8 | latency | 장치(CPU/CUDA) × `W` × batch | — | — | 1,000회 × 설정 (warmup 50) | p99 < 10 ms |
| 7.9 | conformal | 교정 모드(global/mondrian) | **동일 절차를 PCHIP·persistence에도 적용** | val 교정 / test 평가, 둘 다 genuine | 4 | Winkler 점수 우세 seed 수 |
| 7.10 | campaign | split 방식: 무작위 → 시간순 | 무작위 split 헤드라인 | 시간 manifest 고정 + 순서 assert | 4 **init** seed (split 고정) | PR4를 **두 잣대 모두**에 적용 |
| 7.11 | GP | 없음 (오프라인 arm 추가) | — | 얼린 split | 4 | 모델 vs GP PR4 |
| 7.12a | fit-failure | `T_i` 상한 절단값 | **모든 arm에서 동일 행 제거** | 얼린 split | 4 | PASS 유지 여부 |
| 7.12b | peak × held | 없음 (층화) | `T_i`(held ≈0%)가 대조군 | 얼린 split | 4 | 영역별 CI |
| 7.13 | per-shot | shot별 입력 표준화 on/off | 캠페인 base **와** held-free 헤드라인 (2 arm) | 각각의 manifest 고정 | 4 | paired 4/4 CI가 0 배제 |

## 7.1 held 값이 학습까지 오염시키는가 (stuck-free)

**왜.** 최종 모델의 물리 서사("`V_rot`는 10 ms 격자에서 강하게 지속적이므로 과거 CES 이력이 지배적
정보원")가, **관측된 `V_rot`의 54%가 forward-fill인** 데이터에서 학습되었다. held 값이 학습 타겟과
`ces_history` 입력 **양쪽**에 앉아 "이력 복사가 최적"이라고 가르쳤다면, persistence 가정은 부분적으로
**계측기 padding의 인공물이자 순환논증**이다. 이걸 "해결"했던 이전 검사는 단일 seed val 지표 A/B로,
이 프로젝트 자신의 `V_rot` 기준(≥4 seed paired)에 미달이었다.

**설계.** 통제 변수는 **하나** — held 제거를 학습 **과** 평가 양쪽에 적용. 아키텍처는 논문 모델로 동일.
held 제거는 (a) 지도 타겟, (b) `ces_history` 값과 관측 flag, (c) 정규화 통계, (d) 보간 앵커에서 모두
빠지는 것을 의미한다. **파일 split을 control manifest에 고정**한 것이 중요한데, held 제거가 유효 파일
목록을 줄이면 시드 셔플이 재분할되어 control의 test shot이 학습에 새는 일이 실제로 seed 7/123에서
관측됐기 때문이다. control은 held-kept로 학습된 기존 계열, 채점 population은 행 단위 일치를 검증했다.

**결과 (paired, held-free − held-kept):**

| seed | paired `CES_VT` | paired `CES_TI` | 자기 `T_i` vs PCHIP | 자기 `V_rot` vs PCHIP |
|---|---|---|---|---|
| 42 | **+0.030 [+0.001, +0.052] PASS** | +0.072 PASS | +0.238 PASS | +0.226 n.s. |
| 1 | **+0.047 [+0.012, +0.081] PASS** | −0.016 n.s. | +0.184 PASS | **+0.202 PASS** |
| 7 | +0.048 [−0.015, +0.152] n.s. | +0.016 n.s. | +0.291 PASS | +0.143 n.s. |
| 123 | **+0.032 [+0.006, +0.057] PASS** | −0.057 n.s. | +0.221 PASS | +0.210 n.s. |

`V_rot` **4/4 양수, 3/4 CI가 0 배제, 반대 0**, 평균 paired **+0.039**(데이터 수정만으로 MSE 4% 감소).
`T_i`는 영향 없음(평균 +0.004).

**판정: KEEP.** held-free 학습이 이후 모든 실행의 규약이 되었다. 해석: forward-fill padding이 능동적으로
이력 복사를 가르치고 있었다. `V_rot`의 이력 의존은 **진짜 물리이면서 동시에 인공물로 부풀려져 있었고**,
정직한 진술은 둘 다를 정량화한다.

**교차 검증.** 윈도우 스윕이 테스트한 적 없는 축에서 같은 효과를 재현했다: held 제거의 이득이 윈도우
길이에 따라 단조 감소한다(W = 2/3/4/6/8에서 +0.088 / +0.048 / +0.035 / +0.003 / +0.006). held가 있으면
**짧은 윈도우의 유일한 히스토리 슬롯이 종종 정보 없는 복사본**인 반면 긴 윈도우는 진짜 값에 닿는다 —
즉 held는 긴 이력을 보상한 게 아니라 **짧은 윈도우를 벌하고 있었다**.

## 7.2 전체 격자 시퀀스 재프레이밍 v1 (seq)

**왜.** 윈도우 파이프라인의 불규칙 시간축은 **자초한 것**이다 — 관측 타겟이 없는 행을 버렸기 때문이다.
빠른 진단은 10 ms 격자의 100%에서 조밀하므로, 인과 LSTM은 **모든 행**을 먹을 수 있고(라벨 없는 행도
입력 맥락을 제공) 성긴 라벨은 순수하게 손실 쪽 마스크로 처리된다. 그러면 윈도우도, 조합 폭발하는
temporal subset 증강도, 샘플 상한도 전부 사라진다.

**설계.** 0.34M 파라미터 단방향 LSTM. 스텝별 특징 = z-score된 빠른 채널 + Δt + 타겟별
(carry-forward 값 / staleness / has-obs). 블록은 ≥0.5 s 간극에서 분리. masked MSE + 동일한 `T_i` 페널티,
val 기반 early stopping. 평가는 `(file, time)` 키 예측 조회로 기존 채점 코드를 포크해 **동일 population·
동일 기준선·동일 산출 스키마**를 유지했다.

**결과 — 세 개의 paired 비교가 고리를 닫는다 (각 4 seed):**

| 비교 | `CES_TI` | `CES_VT` |
|---|---|---|
| seq(held-kept) vs 윈도우(held-kept) | 4/4 양수, 평균 +0.042 | **2/4 유의하게 나쁨** |
| seq(held-free) vs 윈도우(held-kept) | 평균 +0.039 | 유의차 없음 |
| seq(held-free) vs 윈도우(held-free) | −0.001 / +0.035 / +0.002 / **+0.100\*** | **4/4 유의하게 나쁨** (−0.044 / −0.072 / −0.067 / −0.023) |

**판정: `T_i`는 검증, `V_rot` 결손은 두 원인으로 정확히 분해.** (1) **held 오염** — held-free 학습이
모든 유의한 열세를 제거(1행→2행). (2) **라우팅 부재** — 동일한 held-free 데이터에서도(3행) 공유 인코더
LSTM은 `V_rot`를 4/4 seed에서 잃는다. 이것이 논문 모델의 `V_rot` head 설계(빠른 진단 차단 + 관측 마스킹
이력 attention)가 실제로 우위를 운반함을 확인해 준다. → 다음 통제 변경을 **명시적으로 지목**: seq v2.

## 7.3 seq v2 — 구조에서 유도한 설계의 조립 ★

**왜.** 이 문제가 **무엇과 구조적으로 동형인지**를 먼저 묻는다. 성긴 타겟 회귀가 아니라 **다중 속도
센서 융합 하의 잠재상태 추정**이다: 하나의 플라즈마 상태가 조밀·빠르게(BES/ECEI/MC, 격자의 100%)
그리고 성기고 잡음 있게(CES) 관측된다. 이 동형성은 네 가지 설계를 강제하는데, **이 저장소는 그 넷을
8개월에 걸쳐 각각 따로 발견했다:**

| 동형성이 강제하는 것 | 우리가 발견한 방식 |
|---|---|
| 미관측 시각에도 상태는 존재 → **전체 격자 + 손실 측 마스킹** | §7.2 |
| hold는 관측이 아니다 (값 ≠ 관측 모델) | §7.1 |
| 각 방전은 독립 실현 → **shot별 표준화** | §7.13 |
| 물리적으로 0인 관측 함수는 **구조적으로 차단** | 논문 모델의 `V_rot` 라우팅 |

**설계.** 인과 LSTM 두 개(357,570 params): `T_i`는 22채널 전체 상태를 읽고, **`V_rot`는 non-fast
꼬리만** 읽는다(Δt + 타겟별 carry-forward / staleness / has-obs). 분기는 head가 아니라 **인코더**에서
일어난다 — 공유 순환 상태는 head를 어떻게 배선하든 빠른 진단 정보를 `V_rot` 출력으로 실어나르기
때문이다. **희망이 아니라 구조적 성질로 검증**했다: 빠른 15채널만 섭동하면 `T_i`는 변하고 `V_rot`는
bit-identical로 남는다. control은 §7.1의 held-free 계열(데이터 처리를 공유하는 유일한 control).

**결과 (held-out TEST, genuine, 4 seed). 자기 skill vs PCHIP:**

| seed | 윈도우 control | +shot별 표준화 | **seq_v2** |
|---|---:|---:|---:|
| 42 | +0.238\* | +0.141 | **+0.255\*** |
| 1 | +0.184\* | +0.162\* | **+0.208\*** |
| 7 | +0.291\* | +0.269\* | **+0.305\*** |
| 123 | +0.221\* | +0.254\* | **+0.308\*** |

control과 paired: `T_i` +0.021 / +0.029 / +0.019 / **+0.111\*** → **4/4 양수, 평균 +0.045, 1/4 유의**.
`V_rot` −0.026 / −0.003 / −0.039 / −0.013 → **0/4 유의** (§7.2의 4/4 유의 열세가 사라졌다).

**성분 분해** (라우팅만 켠 arm을 따로 돌림). `V_rot`가 유의하게 나빠진 seed 수:

| arm | 라우팅 | shot별 | `V_rot` 유의 열세 |
|---|:--:|:--:|---|
| v1 | — | — | **4/4** |
| v2_nops | ✓ | — | **1/4** |
| v2 | ✓ | ✓ | **0/4** |

→ **복구는 라우팅이 하고**(4/4→1/4), shot별 표준화가 마지막 seed를 닫는다(1/4→0/4).

**부수 결과.** 학습 비용이 **seed당 1.2–1.4분** (윈도우 파이프라인 12–22분). 윈도우 파이프라인의 비용은
전부 "관측 타겟 없는 행을 버린다"는 초기 결정에서 상속된 것이다 — 그게 시간축을 불규칙하게 만들었고,
윈도우와 조합 폭발 증강과 샘플 상한을 강제했다.

**판정과 그 한계.** 방어 가능한 표현은 **"1/10 비용으로 동급이거나 약간 낫다"**이지 "이긴다"가 아니다:
paired `T_i` 유의는 1/4이고(부호는 일관되나 크기가 seed 잡음 대비 작다), 두 파이프라인의 **학습 예산이
동일하지 않다**(seq는 early stopping, 윈도우는 고정 10 epoch).

**방법론적 함의.** 구조 우선 설계와 한 번에 한 변수 규율은 경쟁이 아니라 **순서**다. 통제 실험은 네 재료
각각을 **개별적으로** 신뢰 가능하게 만든 것이고(넷을 번들로 낸 모델은 held가 학습을 오염시킨다는 것도,
shot별 표준화가 캠페인 전이를 특정해서 고친다는 것도 결코 보여주지 못했을 것이다), 프레이밍은 첫날에도
가능했으며 1/10 비용으로 거의 같은 지점을 가리켰을 것이다. **다음 문제에서는 구조에서 후보 설계를 유도한
뒤 그 부품들을 통제 실험으로 증명하라** — 가장 가까운 지도학습 템플릿에서 출발해 사후 진단으로 구조를
되찾지 말고.

## 7.4 이력은 얼마나 필요한가 (윈도우 스윕)

**왜.** `W = 4`는 근거 없이 놓인 기본값이었다. "2나 3에서도 되는데 왜 4인가"에 답할 기록이 없었다.
이 스윕은 주장 대신 곡선과 **명시적 선택 규칙**을 놓는다.

**설계.** **24회 독립 실행 = W ∈ {2,3,4,6,8} × seed {42,1,7,123}**, 여기에 **history-0** 점 × 4 seed.
논문 모델 고정, 모든 점이 동일한 10-epoch / batch-512 / lr-1e-3 프로토콜, **`W`만 변한다.**
데이터 처리는 **held-free로 명시 고정**(§7.1) — held가 있으면 긴 윈도우가 같은 숫자의 복사본을 더 많이
공급할 뿐이라, "긴 궤적이 회전에 도움된다"는 가짜 신호가 정확히 그 형태로 나온다.

점들을 비교 가능하게 만드는 **세 가지 통제**, 각각 가정이 아니라 실데이터에서 확인:

- **파일 split이 윈도우 불변**임을 W = 2/4/8 × 4 seed에서 직접 확인 — 유효 파일 목록과 train/val/test
  분할이 동일. 사후 확인: **24회 실행 전부가 seed당 동일한 96개 test shot**을 평가한다.
- **shot당 샘플 상한 500** (전역 상한 이전에 적용). temporal subset 증강이 `W`에 따라 조합 폭발하므로
  (240k @ W=2 → 30.1M @ W=8, 파일당 최대 187k) 상한이 없으면 전역 시드 부분집합이 **`W`가 클수록 더
  강하게** 소수의 긴 블록 shot에 지배당해, 비교하려는 대상 자체를 교란한다.
- **평가 population이 거의 불변**: 깨끗한 전체 윈도우 샘플은 연속 `W` 행이 필요하므로 `n`이 `W`와 함께
  줄지만 **−1.8%**(33,900 → 33,288)에 불과하다.

**history-0 점.** `W = 1`은 구현상 불가능(윈도우 ≥2)하므로, **W = 4에서 `ces_history` 텐서를 0으로 둔 채
학습·평가**하는 방식으로 만든다(센서 윈도우와 시간 특징은 W = 4 유지). 이게 W = 1보다 이력을 깨끗하게
격리한다. 이때 **기준선들은 여전히 진짜 이력을 읽으므로** 동일한 잣대에서 측정된다.

**결과:**

| 이력 | W | `CES_TI` 평균 | PASS | `CES_VT` 평균 | PASS |
|---|---|---:|---|---:|---|
| **0** | 4 | **−0.026** | 0/4 | **−0.783** | 0/4 |
| 1 | 2 | +0.238 | **4/4** | **+0.206** | 0/4 |
| 2 | 3 | **+0.246** | **4/4** | +0.203 | 1/4 |
| 3 | 4 *(기존 기본값)* | +0.221 | 3/4 | +0.190 | 1/4 |
| 5 | 6 | +0.190 | 3/4 | +0.205 | 1/4 |
| 7 | 8 | +0.216 | **4/4** | +0.204 | 2/4 |

**읽기 1 — 이력은 필수이고, 그 첫 관측이 사실상 전부를 운반한다.** 이력을 통째로 빼면 `T_i`가 PCHIP
**아래로**(−0.026, 0/4), `V_rot`는 −0.78(PCHIP MSE의 약 1.8배)로 떨어진다. **빠른 진단만으로는 보간
동률에도 못 미친다.** 과거 관측 **하나**가 두 타겟을 동시에 최대치로 끌어올린다.

**읽기 2 — 이력 1개 이후로 곡선이 평평하다, 두 타겟 모두.** `T_i` 평균의 폭은 0.190–0.246(범위 0.056),
`V_rot`는 0.190–0.206(범위 **0.016**)인데, **한 점 안의 seed 산포가 0.07–0.16으로 곡선 전체보다 넓다.**

**판정: `W = 2`.** "plateau에 도달하는 가장 작은 W" 규칙이 W = 2를 준다. **`W = 4`는 skill로 정당화되지
않는다** — 두 타겟 모두에서 W = 2/3보다 낮다. W > 2를 지지하는 유일한 논거는 skill이 아니라 **coverage**다:
채점은 윈도우 **안에** 관측값이 있을 때만 이뤄지므로 W가 작으면 인접 행에 값이 없는 타겟이 빠진다.
총합은 거의 안 변하지만(`V_rot` 52.1k → 53.2k) 어려운 부분집합은 4–10배 커진다(Δt > 15 ms가 456 → 1,958).
**넓은 윈도우는 더 좋은 예측이 아니라 더 많은 장간극 샘플을 예측한다.**

## 7.5 큰 간극에서 약한가 (large-gap, 인과 기준선 대비)

**왜.** 단일 seed 결과에서 `T_i` skill이 넓은 Δt 구간에서 음수로 갔다(Δt > 105 ms에서 −8.98). "공백
채우기가 가장 어려운 곳에서 붕괴한다"로 읽힌다. 함정은 **비교 대상**이다: PCHIP은 **미래 앵커를 받으므로**
간극이 넓어질수록 문제가 *쉬워지고*(진짜 관측 두 개 사이 보간), 모델은 어려워진다(과거만으로 외삽).
게다가 **실시간 nowcaster는 PCHIP을 애초에 돌릴 수 없다.** 결정적 질문은: 큰 간극에서 모델이
**배치 가능한 최선의 기준선(persistence)** 을 여전히 이기는가?

**설계.** 재학습 없음. 채점 코드에 **persistence 제곱오차 키 하나를 가산적으로 추가**하고(기존 키는
불변), 얼린 헤드라인 계열을 **두 처리 모두**에서 재채점하되 **기존 키 전부가 bit-identical로 재현되는지
확인한 뒤에만** 새 키를 받아들인다(8회 실행 전부: 12키 bit-identical, +2 신규). 분석은 **4개 split을
pooling**하고(구간별 표본이 수십 개뿐) bootstrap 클러스터를 **물리적 shot**에 건다(두 split에 등장하는
방전은 클러스터 하나).

**결과 (genuine, 4 split pooled). 굵게 = CI가 0 배제:**

| Δt | n | shots | `T_i` vs PCHIP *(미래 사용)* | `T_i` vs **persistence** *(인과)* |
|---|---:|---:|---|---|
| ≤ 15 ms | 134,629 | 298 | **+0.262** | **+0.407** |
| (15, 25] | 2,987 | 259 | **+0.183** | **+0.384** |
| (25, 35] | 784 | 122 | **+0.402** | **+0.564** |
| (35, 55] | 395 | 98 | +0.143 | +0.169 |
| (55, 105] | 163 | 57 | **+0.282** | **+0.287** |
| > 105 ms | 167 | 62 | **−0.542** | +0.266 |
| **> 15 ms 전체** | 4,496 | 272 | **+0.191** | **+0.388** |
| **> 45 ms 전체** | 435 | 105 | −0.057 (n.s.) | **+0.271** |

**판정 세 가지.** ① **`T_i` 우위는 인접 이력에 국한되지 않는다 — 이건 새 결과다.** pooling이 비인접
영역을 처음으로 측정 가능하게 만들었고, 거기서도 미래를 쓰는 PCHIP을 이긴다(+0.191). "작은 간극에
집중"이라는 이전 서술은 skill 문제가 아니라 **검정력 문제**였다. ② **Δt > 45 ms의 "붕괴"는 CI 앞에서
살아남지 못한다**(−0.057 n.s.). 단일 seed의 −8.98은 n = 45의 잡음이었다. 그리고 인과 기준선 대비로는
거기서 **유의하게 이긴다**(+0.271). 오직 > 105 ms 극단에서만 PCHIP이 유의하게 앞선다 — 진짜 앵커 두 개
사이를 보간하는, 모델은 외삽해야 하는 구간이다. **넓은 간극은 양방향 보간의 영토이고, 온라인 시스템에는
애초에 없는 능력이다.** ③ `V_rot`는 보간과 비기지만 **해상 가능한 모든 간극에서 모든 인과 대안을 이긴다**.

## 7.6 실제 결측점에서도 살아남는가 (MNAR 재가중)

**왜.** "관측점에서만 쟀고 MNAR이라 낙관 상한이다"가 논문 최대의 **수치 없는** 단서였다 — 헤드라인에
크기 미상의 보정이 붙어 있는 셈이다. 이걸 닫는다.

**설계.** 평가 전용. **관측 행과 결측 행 양쪽에서 계산 가능한** 두 공변량으로 사후층화한 뒤, 채점된
점들을 **결측 행의 분포로 재가중**한다. 공변량은 (i) `dt` = 그 타겟의 마지막 **genuine** 관측 이후
경과 시간, (ii) `act` = **입력만의** 국소 활동 flag(이웃 집합이 자기 행을 제외하므로 결측 행에서도
정의된다).

**세 가지 가드가 전부 결과를 좌우한다:**
- **도메인.** 채점은 윈도우 **안에** 관측값이 있을 때만 이뤄지므로, 가장 가까운 과거 관측이 `W−1` 행보다
  멀리 있는 결측 행은 어려운 게 아니라 **모델을 적용할 수 없다.** 그걸 채울 수 있다고 세면 없는 모집단을
  발명하는 것이다.
- **충분성.** 채점 샘플 30개 미만 층은 신뢰하지 않고 버리며, 덮인 질량을 추정치 옆에 항상 보고한다.
  이 가드가 없을 때 `V_rot` 추정치는 **결측 행 15,404개에 채점 샘플 6개**인 층이 끌고 있었다.
- **정렬.** 채점 측은 얼린 산출물에서 `act`를 가져오고 가중치는 결측 행까지 포함한 격자에서 오므로
  임계값이 갈릴 수 있다. 실측 확인: 활동률 0.124 vs 0.128(`T_i`), 0.233 vs 0.241(`V_rot`) — 같은 변수다.
- CI는 가중치를 고정한 채 채점 측에서 shot-clustered bootstrap(B = 2,000). 오차 변동은 덮지만
  **가중치 추정 변동성은 덮지 않는다**(명시).

**결과 1 — 이전에 진술된 적 없는 범위 사실.** 진짜 결측 행 중 W = 4에서 **in-domain**인 비율은
**`T_i` 54.1%, `V_rot` 4.8%** 뿐이다. 이 윈도우에서 이 방법은 실제 `T_i` 공백 문제의 절반쯤과
`V_rot` 문제의 한 구석을 다룬다. 이건 **정확도가 아니라 coverage 한계**이며, 윈도우 문제를 다시 연다:
**W는 skill과 무관하지만 실제 결측 중 얼마나 도달 가능한지는 결정한다.** 이력의 **깊이가 아니라
도달거리**를 분리해야 한다는 가장 강한 논거다.

**결과 2 — in-domain 결측 모집단으로 재가중한 skill** (층 커버리지 95–100%):

| 대상 / 기준선 | seed 42 | seed 1 | seed 7 | seed 123 | 낙관폭 |
|---|---|---|---|---|---|
| `T_i` vs **persistence** | **+0.292** | **+0.308** | **+0.293** | **+0.293** | +0.06…+0.12 |
| `T_i` vs PCHIP | +0.061 | +0.132 | **+0.211** | +0.175 | +0.06…+0.09 |
| `V_rot` vs **persistence** | +0.139 n.s. | **+0.415** | +0.364 n.s. | **+0.269** | −0.05…+0.21 |
| `V_rot` vs PCHIP | −0.014 n.s. | **+0.189** | +0.221 n.s. | −0.230 n.s. | −0.10…+0.41 |

**판정: 인과 주장은 4/4로 살아남고(+0.29), 오프라인 주장은 1/4로 붕괴한다.** 이것이 §1.5의 두 주장을
분리해야 하는 실증적 근거다.

## 7.7 복잡도가 무엇을 사는가 (anchor + Δ 사다리)

**왜.** 외부 피드백 "모델이 너무 복잡하고 설명성이 부족하다". 틀린 답은 잘 되는 작은 모델을 보여주는
것이고, 맞는 답은 **복잡도가 얼마를 버는지 측정**하는 것이다.

**설계.** 타겟별로 **이름 붙은 항 세 개의 합**을 예측하는 모델:

```
ŷ = [(1−s)·y₁ + s·ȳ_obs]          앵커: 가장 가까운 관측 CES, 학습되는 평활 s = σ(α)
  + β · m · g                      마지막 두 관측의 기울기 × 간극
  + Σ_mod rate_mod(stats_mod)·g    모달리티별(BES/ECEI/MC) 변화율 × 간극
```

**1,258 파라미터**(논문 모델의 0.6%). 학습되는 항은 전부 **0으로 초기화**(α = −4)되어 **학습이 정확히
persistence에서 시작**한다 — 그래서 skill은 문자 그대로 학습이 더한 값이다. 항별 기여를 반환하는
분해 함수가 있어 어떤 천이 shot이든 항 단위로 설명할 수 있다. 실행 전에 **프로토콜 결함을 수정**했다:
러너가 데이터 처리를 고정하지 않고 있었다(§7.4의 함정). 이제 학습·평가 모두 held-free로 고정하고,
파일 split을 control manifest에 고정하며 누출 검사를 걸고, **held-free control**과 페어링한다 —
그래야 paired 차이가 데이터 처리가 아니라 아키텍처를 격리한다.

**결과 (held-out TEST, `skill_vs_pchip`, held-free):**

| arm | params | s42 | s1 | s7 | s123 | 평균 |
|---|---:|---:|---:|---:|---:|---:|
| persistence | 0 | −0.260 | −0.320 | −0.251 | −0.256 | **−0.272** |
| **anchor+Δ** | **1,258** | −0.107 | −0.138 | −0.097 | −0.109 | **−0.113** |
| 논문 모델 | 201,258 | +0.238 | +0.184 | +0.291 | +0.221 | **+0.234** |

persistence → 최종 모델 간극 중 앵커가 회수하는 비율: `T_i` **30.7 / 36.1 / 28.4 / 30.8%**(평균 31.5%),
`V_rot` 평균 **7%**.

**판정.** ① 복잡도는 값을 하고 있고, 이제 **얼마인지 말할 수 있다** — 완전히 해석 가능한 0.6% 파라미터
모델이 `T_i` 마진의 ~31%, `V_rot`의 ~7%를 회수한다. 나머지 69% / 93%가 학습된 다중모달 인코더가 사는 것.
② 앵커도 persistence보다는 **한참 위**(−0.113 vs −0.272)라 허수아비가 아니라 정당한 사다리 칸이다.
③ **간극이 모델이 아니라 문제에 대해 알려준다**: 앵커는 **간극에 선형**이다(앵커 + 기울기×간극 +
변화율×간극). 그게 3분의 1만 잡는다는 건 회수 가능한 구조가 상당히 **비선형/비국소**라는 뜻이다 —
더 설명 가능한 모델은 선형 항을 더 쌓는 게 아니라 **다른 함수 형태**가 필요하다. `V_rot`의 7%는
그 신호가 **국소 기울기가 아예 아니라는** 뜻이다.

## 7.8 실시간 가능한가 (지연시간)

**설계.** 네트워크 forward pass만 측정(특징 조립과 정규화는 모델 밖). 1,000회 반복(50회 warmup 후),
**p95/p99를 보고** — 제어 루프는 꼬리가 결정하기 때문이다. per-call 타이밍(장치를 idle로 강제 —
10 ms 루프의 현실적 모형)과 amortized back-to-back 타이밍 **두 방식 모두** 측정.

**결과.**

| W | 장치 | batch | 중앙값 | p99 | 처리량 |
|---|---|---:|---:|---:|---:|
| 2 | **CPU** | 1 | **2.76 ms** | **8.74 ms** | 385/s |
| 4 | **CPU** | 1 | **2.83 ms** | **6.36 ms** | 317/s |
| 2 | CUDA | 1 | 21.70 ms | 42.60 ms | 44/s |
| 4 | CUDA | 1 | 21.45 ms | 72.01 ms | 35/s |
| 2 | CPU | 512 | 10.61 ms | 24.10 ms | **48,390/s** |
| 2 | CUDA | 512 | 18.96 ms | 43.63 ms | 24,305/s |

**판정.** ① **CPU에서 10 ms CES 예산에 들어간다**(p99 6.4–8.7 ms = 한 격자 주기의 64–87%).
"실시간"이 주장이 아니라 측정이 되었다. ② **단일 샘플 추론을 GPU에 올리지 말 것** — CUDA batch-1이
CPU보다 ~8배 느리고 예산을 4.3–7.2배 초과한다. 0.2M 파라미터로는 커널 실행 오버헤드를 상쇄할 계산이
없다. 두 측정 방식이 일치하므로 GPU를 강제로 놀린 탓이 아니다. ③ 실행 간 p99가 6.2 → 8.7 ms로
흔들리므로 정직한 표현은 "노트북 CPU에서 여유가 제한적이지만 예산 안"이다.

## 7.9 재학습 없이 교정된 예측 구간 (split conformal)

**왜.** 오차 막대 없는 가상 센서는 쓸 수 없고, 융합 커뮤니티의 기본 도구(GP 회귀)는 사후분포를
반환하는데 우리는 점을 반환한다. 학습된 분산/분위 head는 **재학습을 요구**하고, 그러면 점 예측이
움직여서 논문의 모든 skill 숫자를 교란한다. **split conformal은 이걸 통째로 회피한다**: 별도 split에서
교정하고, 예측기를 전혀 바꾸지 않으며, 분포 무관 유한표본 marginal coverage를 준다.

**설계.** 점수 = |잔차|. **VAL에서 교정, TEST에서 평가**, 양쪽 다 genuine 처리(디스크의 유일한 val
산출물이 held-kept였으므로 val을 재생성했다 — 한 모집단에서 교정하고 다른 모집단에서 시험하면 보증이
조용히 무효가 된다). 변형 두 가지: `global`(단일 분위) / `mondrian`(dt × 활동 층별 분위, 교정점 50개
미만이면 global로 폴백). α = 0.10. **동일한 절차를 PCHIP과 persistence에도 적용**해서, 차이가 교정
기법이 아니라 구간 품질이 되게 했다.

**결과 (4 seed 평균; Winkler 구간 점수, 낮을수록 좋음).**

| 타겟 | 모드 | coverage | Winkler 최우수 | 폭 (vs pers.) | Winkler (vs pers.) |
|---|---|---|---|---:|---:|
| `T_i` | global | 87.1–91.7% | 모델 4/4 | 0.966 | **0.807** |
| `T_i` | mondrian | 87.0–91.4% | 모델 4/4 | **0.884** | **0.796** |
| `V_rot` | global | 90.8–93.8% | 모델 4/4 | 0.944 | **0.827** |
| `V_rot` | mondrian | 91.0–94.0% | 모델 4/4 | 0.937 | **0.831** |

**판정.** ① 모델 구간이 **proper scoring rule로 두 기준선을 seed·타겟·모드 전부에서(8/8) 압도**한다.
점 추정이 낫다고 자동으로 따라오는 결과가 아니다 — 구간 점수는 폭과 miss를 **함께** 벌한다.
② mondrian이 어디서나 global보다 낫다 — dt와 활동이 필요하다고 말하는 곳에 폭을 배치하는 게 (평균 폭이
늘어도) 이득이다. ③ 정보 비대칭이 불확실성에도 **약하게** 나타난다: 동일 coverage에서 `T_i` 구간은
persistence의 0.884배로 줄지만 `V_rot`는 0.937배에 그친다. ④ **정직한 실패: coverage가 marginal이지
conditional이 아니다.** shot별 coverage가 10분위 ~50–68%에서 90분위 100%까지 흔들린다. conformal은
교환가능성 기반 **marginal** coverage만 보증하는데, 교정과 시험이 **서로 다른 방전**이고 shot 수준
분포 이동이 교환가능성을 깬다. 배치된 계측기에는 이게 통합 수치보다 중요하다 — **어떤 방전에서는
구간이 체계적으로 너무 좁다.**

## 7.10 캠페인(시간) 분할에서도 되는가

**왜.** 여기까지의 모든 결과는 **시드 무작위** 파일 분할 위에 있다. 행 인접 누출에 대해서는 옳은
통제지만, 배치가 마주하는 상황이 아니다: 배치된 nowcaster는 존재하는 shot으로 학습해 **아직 존재하지
않는** shot에서 돈다. 무작위 분할은 모델에게 미래 방전을 학습 재료로 준다.

**설계.** 641 방전을 shot 번호로 정렬(30801–32751, 연속, 200 초과 캠페인 간극 없음)하고 **시간으로만
자른다**: **train 416 [30801, 31991] → val 128 [32002, 32310] → test 97 [32312, 32751]**. 어떤 test
shot도 어떤 train shot보다 앞서지 않음을 assert로 검증. 나머지는 전부 헤드라인 프로토콜에 고정.
**분할이 고정이므로 네 실행은 초기화 seed만 다르다** — 복제가 분할이 아니라 초기화에 대해 이뤄지지만,
각 실행 내부의 shot-clustered CI는 여전히 shot 간 일반화를 덮는다. test 97 shot ≈ 무작위 분할의 96이라
검정력이 비교 가능하다. **두 잣대 모두에 PR4 처리**를 적용했다(기존 도구는 보간에만 게이트를 걸어서,
정작 살아남는 주장이 맨 점추정으로 방치되어 있었다).

**결과.** `T_i` vs PCHIP: +0.051 / +0.044 / −0.148 / −0.018 → **0/4 PASS**.
`T_i` vs **persistence**: **4/4 PASS**(예: seed 42 +0.275 [+0.175, +0.342]). `V_rot`도 persistence
대비 PASS. 평균 인과 우위 **+0.22**.

**판정: 오프라인 주장은 죽고 인과 주장은 살아남는다 — 그리고 원인을 안다.** 캠페인 간 **빠른 진단이
1.22 σ(BES) / 0.53 σ(ECEI) 이동하는 동안 예측 대상은 0.115 σ**만 움직인다. 즉 입력 분포가 모델 밑에서
미끄러진다. 이것은 **train 파일 전용 정규화를 기소**하고 그 수리법을 지목한다 → §7.13.

## 7.11 가장 강한 오프라인 상대 (GP)

**왜.** 채점 도구에는 처음부터 GP arm이 있었지만 라이브러리 부재로 조용히 건너뛰어졌고, 융합 커뮤니티의
표준 프로파일 피팅 방법이 **수사적으로만** 방어되고 있었다.

**설계.** 평가 전용, 재학습 없음. 실행된 적 없던 초안(전 블록 fit + 샘플별 L-BFGS)은 38 ms/fit →
seed당 42분으로 실행 불가였다. **정확 numpy/scipy GP**로 대체: Matern-3/2 + white noise,
**가장 가까운 과거 16 + 미래 16 관측 이웃에 국소 fit**(길이 척도 ≤80 ms에서 사후분포는 더 먼 점에
수치적으로 둔감), 이웃 집합 안에서 값 표준화, 하이퍼파라미터는 **샘플마다 고정 격자 위 정확 로그 주변
우도로 선택**(ls ∈ {5,10,20,40,80} ms × noise ∈ {1e-4…1e-1}) — 완전 결정론적, **0.94 ms/fit**.
결정적으로 이 arm의 NaN 조건이 기존 `ar_local`과 **동일**하므로 **arm 추가가 채점 모집단을 줄일 수 없다**
— 검증: genuine 재실행 4회 전부가 참조 산출물을 **14키 bit-identical + 4 신규키**로 재현.

**결과 (genuine, TEST):**

| seed | RMSE GP / PCHIP / 모델 (`T_i`) | GP vs PCHIP | **모델 vs GP** |
|---|---|---|---|
| 42 | 391.1 / 449.3 / 407.0 | **+0.242** PASS | −0.083 n.s. |
| 1 | 442.4 / 499.7 / 447.8 | **+0.216** PASS | −0.024 n.s. |
| 7 | 490.2 / 550.0 / 466.6 | **+0.206** PASS | **+0.094 [+0.014, +0.153] PASS** |
| 123 | 532.6 / 626.7 / 538.1 | **+0.278** PASS | −0.021 n.s. |

`V_rot`: GP vs PCHIP +0.18…+0.23(3/4 PASS), 모델 vs GP 전부 n.s.

**판정: GP가 결정적으로 최강 오프라인 arm이고, 모델은 GP와 무승부다**(유의 우세 1/4, 유의 열세 0/4,
평균 ≈ −0.01). 부분집합도 무승부를 깨지 못한다(peak 1/4, Δt ≤ 15 ms 1/4, Δt > 15 ms 0/4 — 미래 앵커가
GP를 가장 많이 돕는 구간이라 예상대로). GP가 PCHIP을 이기는 메커니즘은 §5.4의 linear-beats-PCHIP과
같은 계열이다: PCHIP은 잡음 있는 앵커를 **정확히 통과**하고, 잡음 인지 GP는 그 잡음을 평균해 없앤다.

**무엇이 바뀌고 무엇이 안 바뀌는가.** 사전등록된 주장(vs PCHIP)은 유지된다 — 사전등록이 존재하는 이유가
그것이다. 그러나 "미래를 쓰는 보간을 이긴다"를 **"모든 오프라인 방법을 이긴다"로 일반화하면 안 된다.**
정직한 형태: **"인과 모델이 관측 모집단에서 최강의 미래 사용·샘플별 ML 튜닝 smoother와 동률이고,
사전등록된 보간들은 이긴다."** 배치 주장은 영향 없다 — GP는 미래 앵커를 읽으므로 온라인에 존재하지 않는다.

## 7.12 인공물과 하위 모집단 감사 (fit 실패 / peak × held)

**fit 실패 민감도.** `y_true` 키가 산출물에 추가된 뒤 가능해진 평가 전용 분석. **모든 arm에서 동일한
행을 제거**하고 paired로 재계산:

| seed | 전체 skill | ≤3,000 eV 제거 후 | ≤2,089 eV 제거 후 |
|---|---|---|---|
| 42 | +0.179 PASS | **+0.362** PASS (−123행) | +0.364 n.s. (−234행) |
| 1 | +0.197 PASS | **+0.470** PASS | **+0.482** PASS |
| 7 | +0.280 PASS | **+0.589** PASS | **+0.615** PASS |
| 123 | +0.263 PASS | **+0.538** PASS | **+0.575** PASS |

**판정: 인공물은 헤드라인을 부풀리는 게 아니라 깎는다.** 3 keV 위 0.4–0.6% 행을 빼면 4/4 PASS를 유지한 채
skill이 대략 **두 배**가 된다. 비물리적 스파이크는 모든 arm에게 예측 불가라 거대한 제곱오차가 arm 간
비슷하게 나타나 MSE 비를 1로 끌어당긴다. 헤드라인은 그 행들을 **남기므로**(사전등록 모집단에 사후
수술을 하지 않는다) **이 인공물에 대해 보수적**이다. 단 "대략 두 배"라는 크기는 가장 엄격한 절단에서
임계값 민감하다(PASS 판정은 아님).

**peak × held 교차표.** `V_rot`에 대해 한 번도 교차해 본 적 없던 두 사실 — 관측 `V_rot`의 54%가 held이고,
`V_rot`가 활동 peak에서는 PASS하는데 전역은 n.s.라는 것. 서 있던 가설은 "peak는 genuine이 많을 것"
(활동한다는 건 계측기가 실제로 갱신 중이라는 뜻)이었고, 그러면 전역 n.s.가 부분적으로 모집단 인공물이 된다.

**결과는 가설과 반대였다.** **peak는 본류보다 held가 더 많다**(4/4 seed에서 68/62/71/73% vs
58/51/48/46%). forward-fill 계단(평평, 평평, 점프)이 **그 자체로 큰 국소 기울기**라서, 입력만의 활동
탐지기가 부분적으로 **계측기의 hold 패턴에 반응**하고 있다. → 향후 peak 작업은 탐지기를 **genuine 이웃만으로**
다시 계산해야 한다.

**그리고 `V_rot`는 세 영역으로 깨끗이 분해된다**(skill vs PCHIP): **genuine·peak** +0.55…+0.63
(persistence 대비 **+0.75…+0.82, 4/4 PASS**) / **genuine·본류** ≈ 0(매끄러운 구간에서 보간이 거의 최적)
/ **held 행** −48…−411 — 이건 모델 실패가 아니라 **구조적**이다: PCHIP은 정의상 직전 값을 정확히
통과하고 held 행의 정답이 바로 그 값이므로 **어떤 인과 방법도 이길 수 없다.** held 행을 빼면 모든 seed가
오르지만(+0.154→+0.201 등, 4/4) PR4에는 못 닿는다 — held 희석은 실재하나 `V_rot`가 n.s.인 **전부는
아니고**, ~63 shot의 검정력이 나머지다. held가 ≈0%인 `T_i`가 깨끗한 대조군이고 peak 집중을 **4/4 PASS**로
재현하므로, "우위는 고변동 이웃에 산다"는 주장은 이 인공물에 의존하지 않는다.

> **따라서 "`V_rot`는 전역 n.s.인데 peak에서 PASS"라고만 쓰면 안 된다** — 부호가 반대인 영역들의 평균이다.

## 7.13 shot별 입력 표준화 (§7.10이 지목한 수리법)

**왜.** §7.10이 캠페인 전이 실패의 원인을 **측정**했다(빠른 진단 1.22 σ 드리프트 vs 타겟 0.115 σ).
거기서 지목한 수리법 — 빠른 진단을 **각 방전 안에서** 표준화 — 을 실행한다.

**설계.** 로드 시점에 각 shot의 BES/ECEI/MC 채널을 **그 shot 자신의** 평균/σ로 z-score. 따라서 하류의
모든 단계(train 파일 전용 전역 통계, 디스크 캐시, 모델 입력)가 일관된 하나의 처리를 본다.
**타겟은 절대 손대지 않는다** — shot별 타겟 스케일링은 물리 단위 역변환을 shot 의존으로 만들어 모든
기준선 비교를 깨뜨린다. 직접 검증: 플래그를 켜면 파일별 입력 평균/σ = 0.0000/1.0000(끄면 0.07/0.70,
0.00/0.70, 0.12/1.38, 0.12/2.34 — §7.10이 측정한 바로 그 드리프트), 타겟 열의 nanmean은 양쪽에서
bit-identical.

**arm을 둘로 나눈 이유**: 전이를 겨냥한 수리법은 **겨냥한 곳** 과 **논문의 주장이 사는 곳** 양쪽에서
확인해야 한다.

**Part 1 — 캠페인 분할 (겨냥한 곳).** control은 §7.10의 실행 그대로, 동일한 고정 시간 manifest·동일 상한·
동일 held-free 처리·동일 아키텍처, 통제 변수는 shot별 표준화 하나.

| init seed | base | per_shot | paired |
|---|---:|---:|---|
| 42 | +0.051 | **+0.183 PASS** | **+0.139 [+0.057, +0.237]** |
| 1 | +0.044 | +0.135 | **+0.096 [+0.007, +0.199]** |
| 7 | −0.148 | **+0.186 PASS** | **+0.291 [+0.160, +0.420]** |
| 123 | −0.018 | +0.079 | **+0.095 [+0.030, +0.170]** |

**4/4 유의 개선, 평균 +0.155**, 0/4 PASS → 2/4 PASS. control 열이 §7.10을 정확히 재현해 페어링을 확인해 준다.
`V_rot`는 움직이지 않는데(평균 −0.019) 이건 **유용한 음성 대조군**이다: 논문 모델의 `V_rot` head는 빠른
진단으로부터 **구조적으로 차단**되어 있으므로, 그 채널의 표준화가 거기 닿을 수 없다.

**Part 2 — 헤드라인 분할 (주장이 사는 곳).** 겨냥한 곳에서 이겼다고 채택할 수는 없다 — shot별 표준화는
**절대 수준을 버리는데**, 절대 수준 자체가 `T_i` 정보를 담을 수 있다고 논문이 명시하기 때문이다.
control은 **held-free 계열**(held-kept 계열이 아니라 — 그러면 데이터 처리가 통제되지 않은 두 번째
변수가 된다). 결과: **유의한 손실 0/4**(평균 −0.036, 최악 seed 42가 −0.127에 CI 상단 +0.008).

**판정: 채택.** 정직한 표현은 "공짜"가 아니라 **"측정 가능한 비용 없음"**이다. 또한 §7.10의 진단을
**인과적으로** 확인한다: 캠페인 손실이 캠페인 간 물리 변화였다면 입력 *수준* 이동을 제거하는 것으로
회복될 수 없었을 것이다.

**배치 단서.** 온라인 추정기는 shot의 미래를 볼 수 없으므로 shot별 표준화는 이 계열의 **오프라인 상한**이다.
배치 가능한 버전은 확장 윈도우/EWMA 추정기이고, **그 격차는 미측정**이다.

## 7.14 검증된 음성과 곁가지

- **연속시간 이력 인코더 4종** — W = 4에서 전부 음성. 코드는 제거했고 **판정만 남겼다.** 재시도하려면
  W가 달라야 한다는 것이 이 기록의 존재 이유다.
- **파생 Mirnov 특징**(적분, PCHIP 적분, |MC|, rolling RMS) — 학습 + 4 seed paired로 전부 음성.
  §2.3(c)가 이유를 설명한다: **정보가 전처리에서 파괴되었으므로 하류에서 복구 불가.** 재시도 금지.
- **변분 양자 분류기 / IonQ QPU** — 곁가지. 하드웨어는 쓰지 못했고(전 기종 unavailable, QPU job이
  이상 시뮬레이터로 강등), 동일 조건 비교에서 VQC가 고전 MLP에 완패. 논문 주장 사슬에 포함되지 않는다.

---

# 8. 결과를 읽는 규칙

1. **두 주장(§1.5)을 섞지 않는다.** 오프라인 주장은 관측점 집단, 인과 주장은 결측점 집단에 대한 진술이다.
2. **"모든 오프라인 보간을 이긴다"고 쓰지 않는다.** GP와 무승부다(§7.11).
3. **`V_rot`에 대해 "전역 n.s.인데 peak에서 PASS"만 쓰지 않는다.** 부호가 반대인 세 영역의 평균이다(§7.12).
4. **음성 결과는 그것을 뒤집을 측정을 함께 지목할 때만 결론이 된다.** "정보가 부족하다"는 변명이다.
   이 저장소가 지목한 세 레버는 §10에 있다.
5. **윈도우 길이에 따라 스케일하는 결론은 held-free로 재확인하기 전에는 믿지 않는다**(§7.1 교차 검증).
6. **체크포인트를 믿기 전에 자기 기록 지표와 대조한다.** 실제로 어떤 가중치 폴더는 기록된 지표를 만든
   체크포인트가 아니었다(`T_i`는 재현, `V_rot`는 +0.056 vs 기록 +0.161). 증상 패턴: **`T_i`만 재현되고
   `V_rot`가 안 되면 체크포인트 드리프트를 의심하라** — `V_rot`는 CI가 넓어 약간 다른 체크포인트에도
   점추정이 크게 흔들린다.
7. **채점 도구를 우회해 평가 루프를 직접 짜지 않는다.** 기준선이 정의되지 않는 ~160개 `T_i` 샘플이
   버려지는데, 그 몇 개 fit-실패 행이 RMSE를 13% 흔든다.

---

# 9. 한계 (전부 명시적으로 보고됨)

- **통계적 검정력(~96 test shot).** shot이 올바른 독립 단위이고 그 수가 적다. 모든 유의성 판정의
  구속 조건이며 `V_rot`가 해상되지 않는 근본 이유다.
- **무거운 꼬리.** shot별 제곱오차 차이가 heavy-tailed — 소수 방전이 bootstrap 산포를 지배한다.
- **MNAR 낙관 상한.** §7.6이 정량화했지만 CI는 오차 변동만 덮고 **가중치 추정 변동성은 덮지 않는다.**
- **도달거리 한계.** W = 4에서 실제 결측 행 중 in-domain은 `T_i` **54.1%**, `V_rot` **4.8%** 뿐이다.
  정확도가 아니라 **coverage** 한계.
- **이웃 접근 비대칭.** 보간은 shot 전체 이웃을, 모델은 4스텝 이력만 쓴다 — 의도된 설계지만 직접
  해석은 제한된다.
- **얇은 장간극 구간.** Δt > 25 ms 구간은 수십 샘플. §7.5의 pooling이 일부 해소했다.
- **fit 실패 인공물이 평가 모집단에 남아 있다** — 헤드라인을 보수적으로 만드는 방향이다(§7.12).
- **conformal coverage는 marginal이지 conditional이 아니다** — shot별 coverage가 50–100%로 흔들린다.
- **shot별 표준화는 오프라인 상한**이며 배치 가능 버전과의 격차는 미측정(§7.13).
- **`seq_v2`의 학습 예산이 윈도우 파이프라인과 동일하지 않다**(early stopping vs 고정 10 epoch).

---

# 10. 남은 질문

> 우선순위는 2026-08-12 재정렬로 확정되었다(문서 상단 배너, THESIS_RESULTS.md §8v). 아래 1–3이
> B.1(백본 관문 + 인과 GP)에, 4–5는 B.5 재채점 단계에 흡수된다. 어떤 실험도 B.7 프로토콜 감사
> 완료 전에 시작하지 않는다.

**측정으로 결판나는 것 (비용 순):**
1. **`seq_v2` seed 확대.** seed당 1.3분이므로 16 seed = 20분. 4/4 양의 부호(+0.045)가 실재인지 seed
   잡음인지 정산할 수 있는, 기록 전체에서 **가장 싼 미결 질문**.
2. **`seq_v2` vs 윈도우 파이프라인의 학습 예산 균등화.** +0.045를 아키텍처 효과로 인용하기 전 필수.
3. **인과(past-only) GP arm.** §7.11 무승부를 깨는 명시된 tie-breaker(다른 하나는 test shot 수 증가 —
   seed 7이 현재 검정력에서도 효과가 해상 가능함을 보여준다).
4. **peak 탐지기를 genuine 이웃만으로 재계산**(§7.12).
5. **bracket-distance 층화.** 마지막 관측 이후 Δt가 아니라 가장 가까운 **미래** 앵커까지의 거리로
   층화하면, nowcaster의 빠른 진단 정보가 보간 대비 한계 가치를 갖는 영역이 분리된다.

**데이터가 있어야 풀리는 것 — 음성 결과들이 지목한 세 레버:**
6. **NBI 토크/파워 채널 확보.** `V_rot`의 최상위 레버. 현재 데이터셋에 아예 없다(§2.3d).
7. **원본 kHz Mirnov 스트림에서 윈도우 RMS / band power / 모드수.** 정보는 플라즈마에 없는 게 아니라
   전처리가 파괴했다(§2.3c). 파생 특징 재시도는 금지.
8. **이력의 깊이가 아니라 도달거리(reach) 확장.** W는 skill이 아니라 coverage를 산다(§7.4)이고, 실제
   결측의 절반 이상이 애초에 도달 불가다(§7.6). 전체 격자 프레이밍(§7.3)이 이 제약의 프레이밍 몫은
   이미 제거했다.

---

*작성 2026-08-10. 모든 수치는 얼린 실행 산출물에서 수집되었다.*
*2026-08-14 갱신: 2026-08-12 방향 재정립(전면 재실험 결정)을 상단 배너와 §10에 반영 — 수치 본문은
W = 4 프로토콜의 기록으로 유지된다.*
