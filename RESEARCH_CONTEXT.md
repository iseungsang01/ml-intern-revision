# RESEARCH_CONTEXT — 실험을 이해하기 위한 컨텍스트

이 저장소의 **실험이 무엇을 왜 하고, 무엇이 확정되었으며, 각 숫자가 어떤 조건에서 나온 것인지**만
담는다. 실행 방법·환경변수·산출물 목록 같은 운영 정보는 여기 없다 (`CLAUDE.md`, `README.md` 참조).

**파생 문서다.** 충돌하면 `THESIS_RESULTS.md`(결과 원장, §8이 실험 1건당 1절) →
`PROJECT_KNOWLEDGE.md`(장기 교훈) → `docs/paper/paper_numbers.json`(논문 수치 단일 출처) 순으로
원본이 이긴다. 새 사실은 §8에 먼저 기록하고 여기에는 반영만 한다.

---

## 1. 한 문단 요약

KSTAR에서 **CES(Charge Exchange Spectroscopy)** 는 이온 온도 `CES_TI`와 토로이달 회전 `CES_VT`를
주지만, 10 ms 격자 위에서 성기고 결측이 많다. 이 연구는 같은 시각의 **빠른 진단(BES 밀도요동 /
ECEI 전자온도 / MC Mirnov 자기요동)** 과 **과거 CES 이력**만으로 그 시점의 CES를 채우는 **인과적
nowcasting 모델**을 만들고, **미래 값까지 쓰는 오프라인 보간**(linear / PCHIP / local AR / GP)이라는
일부러 불리한 잣대에 붙였다. 결론: **`CES_TI`는 미래를 보는 보간을 4개 독립 split 전부에서 유의하게
이긴다.** **`CES_VT`는 전역적으로 비긴다(n.s.)** — 그리고 이 `T_i` ↔ `V_rot` 비대칭이 물리적으로
예측 가능하고 ablation으로 확인된 **발견 자체**다. 인과 기준선(persistence/AR) 대비로는 둘 다 크게 이긴다.

이 문제는 프레이밍상 **super-resolution이 아니라 10 ms gap-filling nowcasting**이다.

---

## 2. 문제 정의와 두 개의 주장

**질문.** 10 ms 타겟 시점에서, 동시각 빠른 진단 + 과거 CES 이력만으로 CES 정보를 —
**CES 자체의 시간 보간이 복원할 수 있는 것 이상으로** — 복원할 수 있는가?

**잣대.** 기준선은 타겟 주변의 **과거+미래** CES를 모두 쓰는 오프라인 보간이다. 모델은 미래를 보지
못한다. 미래를 보는 상대를 이기면 어떤 인과적 방법이든 a fortiori 이긴다.

**결과로 확립된 주장은 두 개이고, 절대 섞지 않는다:**

| | 주장 | 어떤 집단에 대한 진술인가 | 상태 |
|---|---|---|---|
| **오프라인** | 미래를 쓰는 보간을 이긴다 | **관측된** CES 점 | `CES_TI`만 성립 (4/4). GP와는 무승부 |
| **인과/배치** | 배치 가능한 모든 인과적 방법을 이긴다 | **실제로 결측인** 점으로 재가중해도 생존 | 두 타겟 모두 성립 (§8i, `CES_TI` 4/4 +0.29) |

온라인 가상센서의 경쟁자는 persistence이지 미래를 읽는 보간이 아니다. 이 둘을 섞는 것이 이 결과를
과대판매하는 유일한 경로다.

**검증 방식의 성격.** 진짜 결측 지점에는 참값이 없다. 그래서 **관측된 CES를 일부러 가린 뒤** 얼마나
복원하는지로 채점한다. 결측이 무작위라는 보장이 없으므로(MNAR) 이는 낙관 상한이고, §8i가 그 낙관을
+0.06…+0.12로 **정량화**했다.

---

## 3. 데이터의 실체

KSTAR 방전 **641 shot**, 10 ms 격자 총 **247,207 행**. 컬럼 18개: `time`, `CES_TI`, `CES_VT`,
`BES_*` ×9, `ECEI_*` ×4, `MC*` ×2.

### 3.1 결측 장부

| | `CES_TI` | `CES_VT` |
|---|---:|---:|
| ① NaN 결측 | 20,216 (8.2%) | 59,107 (23.9%) |
| ② held / forward-fill (직전 관측과 bit-identical) | 1 (0.0%) | 101,604 (41.1%) |
| **실질 결측 ①+②** | **20,217 (8.2%)** | **160,711 (65.0%)** |
| 관측값 중 held 비율 | 0.0% | **54.0%** |
| held run 포함 shot | 1 / 641 | 499 / 641 |
| held run 길이 (중앙/최대) | 2 / 2 행 | 10 / **1,214** 행 |

> **"`CES_VT`는 24% 결측"을 단독으로 인용하지 말 것.** 독립 정보가 없는 격자 비율은 **65.0%** 다.

### 3.2 데이터에 박혀 있는 세 가지 인공물 — 실험 여러 개가 여기서 나왔다

1. **held(forward-fill) 값.** 계측기 padding이다. 학습 타겟과 `ces_history` 입력을 **동시에** 오염시켜
   "이력 복사가 최적"이라고 가르친다 → `V_rot`의 persistence 서사가 부분적으로 **순환논증**이었다.
   제거가 이득임을 4/4 seed로 확인(§8c).
2. **CES 스펙트럼 fit 실패.** `CES_TI` > 3 keV 행(0.4–0.6%, 최대 14,984 eV; 전역 p99 = 2,089 eV)은
   물리가 아니라 피팅 실패다. 모든 arm에게 예측 불가라 MSE 비를 1로 끌어당긴다 → 빼면 skill이 약 2배.
   즉 이 인공물을 남긴 **헤드라인은 보수적**이다(§8q).
3. **Mirnov(MC) 앨리어싱.** kHz `dB/dt`를 anti-aliasing 필터 없이 100 Hz로 데시메이트했다.
   블록 내 lag-1 자기상관: BES **+0.568** / ECEI **+0.572** / **MC −0.009**(|r|<0.1이 82%).
   같은 격자 위에서 MC만 백색잡음이다. **정보가 모델에 도달하기 전에 파괴**되었으므로 파생 MC
   특징(적분·|MC|·rolling RMS 등)으로는 복구되지 않는다 — 실제로 시도했고 전부 음성이다.

### 3.3 데이터에 **없는** 것 — `CES_VT`의 진짜 레버

**NBI 토크 채널이 데이터셋에 아예 없다.** `T_e`(ECEI 평균)로 대리하려는 시도는 데이터가 반증한다:
shot 간 `T_e`~`CES_TI` r = **+0.353**(p = 2.9e−17) vs `T_e`~`CES_VT` r = **+0.024**(p = 0.58).
shot 내에서도 같다(+0.246 일관된 부호 vs +0.006 무작위 부호). 사슬의 앞 절반(NBI→전자가열→`T_e`)은
데이터가 확인해주기 때문에 뒤 절반의 null이 정보가 된다. 끊기는 지점은 **power ≠ torque**:
토크는 빔 에너지·접선반경·주입 기하에 의존하고, 회전은 운동량 수송과 edge braking(NTV, error
field, 벽 항력)이 지배한다.

---

## 4. 학습 샘플이 무엇인가 (데이터/모델 계약)

실험들이 조작하는 대상이 바로 이 계약이므로, 이걸 모르면 §8을 읽을 수 없다.

- `model.forward(bes, ecei, mc, time_features=None, ces_history=None)` → 정규화된
  `[CES_TI, CES_VT]`, shape `(batch, 2)`. 역정규화는 모델 밖에서.
- BES/ECEI/MC/타겟은 **train 파일에서만** 구한 통계로 채널별 z-score. CES가 성기므로 타겟 통계는
  **NaN-aware**(관측값 위에서만).
- `ces_history` = `(batch, window, 4)`: 이전 정규화 `CES_TI`, 이전 정규화 `CES_VT`,
  `CES_TI` 관측 flag, `CES_VT` 관측 flag. 두 타겟은 **독립적으로** 결측(≈8% / ≈24%)되므로 관측을
  타겟별로 추적한다.
- **타겟 시점은 값과 flag 모두 0으로 완전 마스킹** — 누출 방지. 이걸 깨면 결과 전체가 무효다.
- 샘플마다 `target_mask` `(batch, 2)`, 학습/검증은 **타겟별 masked MSE**. 한쪽만 관측된 행도 그쪽은
  지도한다 (양쪽 모두 요구하던 예전 코드는 라벨 행의 ≈28%를 조용히 버렸다).
- time features 4채널: lookback 초, delta 초, `log1p` lookback, `log1p` delta — 불규칙 샘플링을 인코딩.
- **split은 행이 아니라 파일(shot) 단위.** 인접 행이 강하게 상관되므로 행 단위 split은 누출이다.
- **temporal subset 증강**: 한 타겟 행을 과거 행들의 여러 불규칙 부분집합과 짝지어 증강한다.
  조합 폭발이라(W=2에서 240k → W=8에서 30.1M) 샘플 상한이 필수이고, 이것이 윈도우 비교를
  까다롭게 만드는 원인이다.

---

## 5. 모델

논문 아키텍처는 `model_iter009.py`(201,258 params), 후기 융합 multimodal. 장식이 아니라
**결과를 만드는** 요소는 두 개다:

- **관측 마스킹 multi-head attention pooling.** 각 타겟의 이력 readout이 **그 타겟이 실제로 측정된**
  시점에만 가중치를 줄 수 있다. 보간을 강하게 만드는 귀납편향을 파라미터 0으로 추가한 것이고,
  §6의 progression(n.s. → 유의)을 만든 변화가 정확히 이것이다.
- **타겟 인지 라우팅.** `CES_TI`는 빠른 진단+이력+시간을, **`CES_VT`는 이력+시간만** 본다.
  물리적으로 0인 관측 함수는 **head가 아니라 encoder에서** 막아야 한다 — 공유 순환상태는 head를
  어떻게 배선하든 막힌 정보를 실어나른다(§8t가 이를 인과적으로 확인).

비교용 "before" 모델은 `model_iter002.py`(GRU + 마지막 은닉상태 readout). 두 파일 모두 SHA-256으로
고정되어 있고 편집 대상이 아니다.

---

## 6. 평가 프로토콜 — 숫자를 읽기 전에

- **3-way 파일 단위 split** (train/val/**test**). TEST는 아키텍처 탐색 전에 격리되어 선택에 쓰이지
  않았다. seed 42/1/7/123 중 **1/7/123은 모델 선택에 한 번도 사용되지 않았다** → 4/4 PASS는
  선택 편향 없는 복제다.
- **지표**: 물리 단위 타겟별 RMSE + Murphy skill `skill = 1 − MSE_model / MSE_baseline`.
- **사전등록(PR)** — TEST 숫자를 보기 전에 고정:
  - **PR1** 헤드라인 기준선 = **PCHIP**(ELM/sawtooth 강건성 이유로 사전 선택). 전체 사다리도 보고.
    *정직한 방어*: PCHIP은 이 데이터에서 linear보다 **약하다**(431.81 vs 422.66). 방어는
    "PCHIP이 최강"이 아니라 "사전등록했고, 더 강한 linear로 바꿔도 결론이 유지된다"이다(4/4 PASS).
  - **PR2** 보간은 모델이 채점되는 모든 관측 타겟에서 예측하고, 미래 이웃이 없으면 persistence로
    폴백한다(≥0.5 s 간극은 보간 거부) → 어느 arm도 표본이 얇아지지 않는다.
  - **PR3** TEST 사전 격리, 하한 ≥15 shot & ≥3,000 관측 `CES_TI` 샘플.
  - **PR4** **shot-clustered paired bootstrap**(B = 10,000, seed 12345). 한 방전 내 인접 행은
    자기상관이므로 **shot이 재표집 단위**다. 95% CI가 0을 모델 쪽으로 배제 = **PASS**.
    이 CI는 shot 간 일반화를 반영하며, split seed 변동은 부차적 안정성 점검이지 CI에 합치지 않는다.
- **평가 처리(treatment) 두 가지를 항상 함께 본다**: `genuine`(held 제외, **헤드라인**) /
  `stuck0`(held 포함, 역사적 관행 — `CES_VT` RMSE를 35–55% 깎아내림). 하나를 인용하면서 다른 하나를
  주장하는 오류가 실제로 발견된 적 있다(§8h).
- **통제 규율**: 한 번에 한 변수, 4개 독립 split, paired bootstrap. **단일 seed 결과는 증거가 아니다.**

---

## 7. 핵심 결과

### 7.1 헤드라인 (held-out TEST, `skill_vs_pchip`, genuine 평가)

| target | seed 42 | seed 1 | seed 7 | seed 123 | 평균 | PR4 |
|---|---:|---:|---:|---:|---:|---|
| **`CES_TI`** | **+0.179** | **+0.197** | **+0.280** | **+0.263** | **+0.230** | **4/4 PASS** |
| `CES_VT` | +0.203 | +0.162 | +0.100 | +0.183 | +0.162 | 1/4 (seed 1만) |

(`stuck0` 처리에서는 `CES_TI` +0.257 / +0.194 / +0.263 / +0.280, 역시 4/4 PASS.)

### 7.2 RMSE 사다리 (seed 42, 물리 단위, `stuck0`)

| arm | 접근 권한 | `CES_TI` | `CES_VT` |
|---|---|---:|---:|
| **모델 (nowcaster)** | 빠른 진단 + 과거 CES | **372.32** | **22.53** |
| linear 보간 | 과거+미래 | 422.66 | 24.01 |
| PCHIP 보간 *(헤드라인)* | 과거+미래 | 431.81 | 24.49 |
| persistence | 마지막 관측값 | 487.31 | 27.77 |
| AR (local) | 과거만 | 1005.66 | 57.23 |

모델이 **두 타겟 모두 사다리 최저 RMSE**. genuine 평가에서 `CES_VT`의 실제 RMSE는 ~35–47이다
(위 22.53은 held 때문에 눌린 값).

### 7.3 비대칭의 메커니즘 — 입력 modality ablation (val, `skill_vs_persistence`)

| ablation | `CES_TI` | `CES_VT` |
|---|---:|---:|
| Full (이력+빠른진단+시간) | +0.428 | +0.296 |
| `no_fast` (이력만) | +0.458 | +0.295 |
| `no_history` (빠른진단만) | **+0.372** | **−0.642** |

빠른 진단만으로도 `CES_TI`는 persistence를 크게 이긴다 — 충돌 e–i 결합(ECEI가 `T_e`, BES가 `n_e`)
때문에 물리적으로 예측되는 결과다. 같은 모델이 `CES_VT`에서는 persistence보다 **나쁘다**(−0.642):
회전 정보는 사실상 전부 과거 CES 이력에서 온다. **이것이 비대칭의 메커니즘이다.**

### 7.4 skill이 어디에 사는가

- **고변동("peak") 이웃에 집중.** peak는 타겟 자신의 값을 제외한 **입력만의 프록시**로 정의되어
  비순환적이다. val: `CES_TI` 전역 +0.272 → peak **+0.702**(PASS), `CES_VT` +0.131 → **+0.438**(PASS).
  보간은 매끄러운 구간에서 거의 최적이고, 모델의 값어치는 활동 구간에 있다.
- **간극(Δt)별.** 4 split을 pooling하면 `CES_TI`는 Δt > 15 ms 전체에서 **+0.191 [+0.10, +0.28]** 로
  미래를 쓰는 PCHIP까지 이긴다. Δt > 105 ms 극단 구간에서만 PCHIP이 유의하게 앞선다 —
  **넓은 간극은 양방향 보간의 영토**이고, 온라인 시스템에는 애초에 없는 능력이다.
  (단일 seed에서 보이던 −8.98 붕괴는 n=45의 잡음이었고 CI를 붙이면 사라진다.)
- **인과 기준선 대비**는 모든 해상 가능한 간극에서 두 타겟 모두 유의하게 이긴다
  (`CES_TI` +0.407 / +0.388, `CES_VT` +0.368 / +0.309).
- **`CES_VT`는 하나의 판정이 아니라 세 영역이다**(§8r): genuine·peak +0.55…+0.63(persistence 대비
  +0.75…+0.82, **4/4 PASS**) / genuine·bulk ≈ 0 / held 행 −48…−411. 마지막은 모델 실패가 아니라
  **구조적**이다 — PCHIP은 정의상 직전 값을 정확히 통과하므로 어떤 인과 방법도 이길 수 없다.
- **가장 강한 상대는 GP.** 국소 Matern-3/2+white GP(샘플별 grid-ML)가 PCHIP을 +0.21…+0.28로 이기고
  (`CES_TI` 4/4 PASS), **모델 vs GP는 무승부**(1/4 PASS, 0/4 반대, 평균 ≈ −0.01).

---

## 8. 통제 실험 대장

| §8 | 실험 | 질문 | 판정 |
|---|---|---|---|
| 8c | `stuckfree` | held 값이 *학습*까지 해치는가? | **KEEP** — `CES_VT` 4/4 개선(3/4 유의), `CES_TI` 무손실. 평균 paired +0.039 |
| 8d·**8t** | `seq` | 전체 격자 시퀀스 재프레이밍이 윈도우 방식을 이기는가? | **8t `seq_v2`: 4/4 split 최고 `CES_TI`, `V_rot` 결손 해소, 학습비용 1/10** |
| 8e | *(ct, 코드 제거됨)* | 연속시간 이력 encoder 4종 | **검증된 음성.** 판정만 §8e에 보존 — W=4에서 재시도 금지 |
| 8f | `window_sweep` | 이력이 얼마나 필요한가? | **과거 관측 1개면 전부.** W=4는 skill로 정당화 불가(W=2에서 이미 plateau), coverage로만 정당화 |
| 8g | `largegap` | 큰 간극에서 약한가? | 아니다 — 그건 보간의 영토. *인과* 기준선 대비로는 거기서도 유의하게 이김(+0.271) |
| 8i | `mnar` | 실제 결측점으로 재가중해도 살아남는가? | 인과 주장 4/4 생존(+0.29), **오프라인 주장 1/4 붕괴**. 낙관폭 +0.06…+0.12 |
| 8k | `anchor` | 복잡도가 무엇을 사는가? | 1,258-param 완전설명 모델이 `CES_TI` 마진의 **31.5%**(`CES_VT` 7%)만 회수 |
| 8l | `latency` | 실시간 가능한가, 어느 장치에서? | **CPU** batch-1 p99 = **6.4 ms**(10 ms 예산 내). **CUDA가 8× 느림**(파라미터 0.2M이라 launch overhead를 상쇄할 게 없음) |
| 8m | `uq` | 재학습 없이 교정된 구간을 얻는가? | split conformal이 두 기준선을 Winkler로 **8/8** 압도. 단 coverage는 **marginal**이지 conditional 아님(shot별 50–100%) |
| 8n | `campaign` | 엄격한 시간(캠페인) split에서도 되는가? | **오프라인 우위 사망, 인과 우위 생존**(+0.22). 원인 측정: BES가 1.22σ 드리프트 vs 타겟 0.115σ |
| 8p | `gp` | 최강 오프라인 상대와 붙으면? | GP가 PCHIP을 이기고 **모델은 GP와 무승부** |
| 8q | `fitfail` | fit 실패(>3 keV)가 헤드라인을 부풀리는가? | 반대로 **깎는다** — 제거하면 skill 약 2배(+0.18…+0.28 → +0.36…+0.59) |
| 8r | `heldpeak` | `CES_VT` peak 결과와 hold 패턴이 얽혔는가? | 얽혔고 **가설과 반대**(peak가 hold-**부자**, 4/4). `CES_VT`는 세 영역으로 분해됨 |
| 8s | `pershot` | shot별 입력 표준화가 캠페인 전이를 고치는가? | **ADOPT** — 캠페인 `CES_TI` 평균 +0.155(4/4 유의), 헤드라인에선 유의한 손실 0/4 |
| — | `quantum` | (곁가지) VQC / IonQ QPU | Forte 전 기종 `unavailable`, 동일조건에서 VQC가 고전 MLP에 완패. 논문 주장 아님 |

### 8.1 `seq_v2` — 구조에서 설계를 유도한 결과 (§8t)

이 문제는 성긴 타겟 회귀가 아니라 **다중 속도 센서 융합 하의 잠재상태 추정**이다: 하나의 플라즈마
상태가 촘촘·빠르게(BES/ECEI/MC, 격자의 100%) 그리고 성기고 잡음 있게(CES) 관측된다. 이 동형성이
강제하는 4가지 설계를, 이 저장소는 8개월에 걸쳐 각각 **따로** 발견했다:

| 동형성이 강제하는 것 | 우리가 발견한 방식 |
|---|---|
| 미관측 시각에도 상태는 존재 → **전체 격자 + 손실 측 마스킹** | §8d |
| hold는 관측이 아니다 (값 ≠ 관측 모델) | §8c |
| 각 방전은 독립 실현 → **shot별 표준화** | §8s |
| 물리적으로 0인 관측 함수는 **구조적으로 차단** | iter009의 `V_rot` 라우팅 |

넷을 모두 조립한 `seq_v2`(인과 LSTM 2개, 357,570 params)는 **4/4 split에서 최고 `CES_TI`**
(+0.255 / +0.208 / +0.305 / +0.308, 4/4 PASS), §8d의 4/4 유의 `V_rot` 결손을 **0/4로 해소**,
학습은 **seed당 1.2–1.4분**(윈도우 파이프라인 12–22분).

분해 실험(`seq_v2_nops`)이 원인을 특정했다 — `CES_VT`가 유의하게 나빠진 seed 수:
**v1 4/4 → 라우팅만 1/4 → 라우팅+shot별 0/4.** 즉 **복구는 라우팅이 하고**, shot별 표준화가 마지막
seed를 닫는다.

**방어 가능한 표현은 "1/10 비용으로 동급이거나 약간 낫다"이지 "이긴다"가 아니다** — paired `CES_TI`
유의는 1/4이고, 두 파이프라인의 학습 예산이 동일하지 않다(seq는 early stopping, 윈도우는 고정 10 epoch).

**방법론적 교훈.** 통제 실험과 구조-우선 설계는 경쟁이 아니라 **순서**다. 각 재료를 개별적으로
신뢰할 수 있게 만든 것은 통제 실험이지만, 프레이밍은 첫날에도 가능했고 1/10 비용으로 거의 같은
지점을 가리켰을 것이다. **다음 문제에서는 구조에서 후보 설계를 유도한 뒤 그 부품들을 통제 실험으로
증명하라** — 가장 가까운 지도학습 템플릿에서 시작해 사후 진단으로 구조를 되찾지 말고.

---

## 9. 결과를 해석할 때의 규칙

1. **두 주장(§2)을 섞지 않는다.** 오프라인 주장은 관측점 집단, 인과 주장은 결측점 집단에 대한 진술이다.
2. **"모든 오프라인 보간을 이긴다"고 쓰지 않는다.** GP와 무승부다. 정직한 형태: *사전등록 보간(PR1)은
   이긴다 / 미래를 쓰는 최강 ML-튜닝 smoother와는 비긴다 / 인과·배치 주장은 영향 없다*(GP는 미래
   앵커가 필요하므로).
3. **`CES_VT`에 대해 "전역 n.s.인데 peak에서 PASS"만 쓰지 않는다.** 부호가 반대인 영역들의 평균이므로
   §8r의 3영역 분해와 함께 서술해야 한다.
4. **데이터 처리를 매 실행마다 명시적으로 고정한다.** `CES_DROP_STUCK_TARGETS`를 상속하거나 `pop`
   하지 말 것. 조용한 기본값 폴백이 window sweep의 결론을 통째로 틀리게 만든 적 있다(§8f: held가
   짧은 윈도우를 불리하게 만들어 "V_rot은 긴 이력이 필요하다"는 인공적 기울기를 만들었고, held-free로
   다시 보니 기울기가 사라졌다). **윈도우 길이에 따라 스케일하는 결론은 held-free로 재확인 전엔 믿지 않는다.**
5. **같은 처리로 학습된 control과 짝짓는다** (held-free arm은 held-free control과). 그리고 채점된
   population이 **행 단위로 일치**하는지 검증한다 — paired bootstrap 도구가 이걸 강제한다.
6. **체크포인트를 믿기 전에 자기 기록 지표와 대조한다.** 실제로 어떤 가중치 폴더는 기록된 지표를
   만든 체크포인트가 아니었다(`CES_TI`는 재현, `CES_VT`는 +0.056 vs 기록 +0.161). 증상 패턴:
   `CES_TI`는 재현되는데 `CES_VT`만 안 되면 체크포인트 드리프트를 의심하라 — `CES_VT`는 CI가 넓어
   약간 다른 체크포인트에도 점추정이 크게 흔들린다.
7. **음성 결과는 그것을 뒤집을 측정을 함께 지목할 때만 결론이 된다.** "정보가 부족하다"는 변명이다.
   이 저장소가 지목한 세 레버는 §11에 있다.

---

## 10. 명시된 한계

- **통계적 검정력(~96 test shot).** shot이 올바른 독립 단위이고 그 수가 적다. 모든 유의성 판정의
  구속 조건이며 `CES_VT`가 해상되지 않는 근본 이유다.
- **무거운 꼬리.** shot별 제곱오차 차이가 heavy-tailed — 소수 방전이 bootstrap 산포를 지배한다.
- **MNAR 낙관 상한.** §8i가 재가중으로 정량화했지만, CI는 오차 변동만 덮고 weight 추정 변동성은
  덮지 않는다.
- **도달거리 한계.** W = 4에서 실제 결측 행 중 in-domain인 비율은 `CES_TI` **54.1%**, `CES_VT`
  **4.8%** 뿐이다. 정확도 한계가 아니라 **coverage 한계**다.
- **이웃 접근 비대칭.** 보간은 shot 전체 이웃을, 모델은 `window_size = 4` 이력만 쓴다(의도된 설계지만
  직접 해석은 제한된다).
- **얇은 큰 간극 구간.** Δt > 25 ms 구간은 수십 샘플. §8g의 pooling이 일부 해소했다.
- **fit 실패 인공물이 평가 집단에 남아 있다** — 헤드라인을 보수적으로 만드는 방향이다(§8q).
- **conformal coverage는 marginal이지 conditional이 아니다** — shot별 coverage가 50–100%로 흔들린다.
- **shot별 표준화는 오프라인 상한이다** — 온라인 추정기는 shot의 미래를 볼 수 없다. 배치형
  (expanding window / EWMA) 버전과의 격차는 미측정.

---

## 11. 남은 질문 (음성 결과가 지목한 레버 포함)

**측정으로 결판나는 것 (비용 순):**
1. **`seq_v2` seed 확대** — seed당 1.3분이므로 16 seed = 20분. 4/4 양의 부호(+0.045)가 실제인지
   seed 노이즈인지 정산할 수 있는, 기록 전체에서 **가장 싼 미결 질문**.
2. **`seq_v2` vs 윈도우 파이프라인의 학습 예산 균등화** — +0.045를 아키텍처 효과로 인용하기 전 필수.
3. **인과(past-only) GP arm** — §8p 무승부를 깨는 명시된 tie-breaker(다른 하나는 shot 수 증가).
4. **peak 탐지기를 genuine 이웃만으로 재계산** — 현재 탐지기가 hold 계단에 부분적으로 반응한다(§8r).
5. **bracket-distance 층화** — 마지막 관측 이후 Δt가 아니라 가장 가까운 *미래* 앵커까지의 거리로
   층화하면 nowcaster의 한계 가치가 가장 큰 영역이 분리된다.

**데이터가 있어야 풀리는 것 — 음성 결과들이 가리키는 세 레버:**
6. **NBI 토크/파워 채널 확보** — `CES_VT`의 최상위 레버. 현재 아예 없다(§3.3).
7. **원본 kHz Mirnov 스트림에서 윈도우 RMS / band power / mode number** — 정보는 플라즈마에 없는 게
   아니라 전처리가 파괴했다(§3.2). 파생 특징 재시도는 금지.
8. **이력의 깊이가 아니라 도달거리(reach) 확장** — W는 skill이 아니라 coverage를 산다(§8f)이고,
   실제 결측의 절반 이상이 애초에 도달 불가다(§10). 전체 격자 프레이밍(§8t)이 이 제약의 프레이밍
   몫은 이미 제거했다.

---

*작성 2026-08-10. 근거: `THESIS_RESULTS.md` §1–§9 및 §8b–§8u, `PROJECT_KNOWLEDGE.md`,
`docs/paper/paper_numbers.json`. 수치를 인용할 때는 이 문서가 아니라 원본을 재확인할 것.*
